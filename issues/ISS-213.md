# ISS-213: Complete external host-boundary integration

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/examples, area/docs, area/lane, integration, tests
- Assignee: Codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description
Add end-to-end source examples and public documentation for user-declared External Effects, External Types, typed host registration, lifecycle behavior, and cross-backend execution.

## Design
Use valid Lane syntax and complete-value inspect tests. Demonstrate one mutable host object API with explicit cleanup and one custom External Effect. Keep deferred recoverable errors, generic External Types, cross-thread transfer, and structured aggregate ABI out of the implementation.

## Acceptance Criteria
- [x] Valid and invalid Lane examples lock the declaration and extern-signature surface.
- [x] LoisVM README documents primitive and `Opaque` host registration and ownership.
- [x] Production interpreter and Wasmoon paths run an External Type example with matching output.
- [x] Canonical contexts and ADRs match implemented public behavior without stale primitive-only claims.
- [x] `moon info && moon fmt`, full checks/tests, example execution, and `git diff --check` pass.
- [x] ISS-205 closes with complete evidence and deferred work remains explicitly recorded.

## Relationships
- Depends on: ISS-211, ISS-212, ISS-214, ISS-215
- Parent: ISS-205
- Related: ISS-204
- Discovered from: ISS-205

## Notes
- 2026-07-16: Started final source-example, embedding-documentation, and cross-backend integration work after ISS-211 and ISS-212 closed.

## Close Notes
Added checked valid and invalid Lane examples for External Types, External Effects, and direct extern ABI restrictions. Expanded the LoisVM README with tested typed registration for primitive and `Opaque` values, explicit cleanup, shared ARC ownership, finalization, trusted projection, and resource limits. Added a production source-to-bytecode conformance test that executes one mutable External Type through both the interpreter and Wasmoon with identical imports, observations, explicit-close count, and finalizer count.

Canonical vocabulary and ADR wording now describe direct primitive-or-`Opaque` calls rather than a primitive-only host boundary. Deferred recoverable failures, generic External Types, cross-thread transfer, and structured aggregate ABI work is recorded in ISS-216.

Verification: `moon info && moon fmt`; `moon check --target all -d`; `moon test` (5 wasm, 633 wasm-gc, 193 native); `tools/check-lane-run-examples.sh`; focused LoisVM README and production External Type tests; stale-term search; `git diff --check`.

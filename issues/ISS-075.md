# ISS-075: Typecheck and normalize effect set aliases

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/typecheck, area/effects, area/type-system, agent
- Assignee: unassigned
- Created: 2026-07-03
- Updated: 2026-07-03
- External ref: none

## Description
Allow top-level type aliases to infer Effect kind and transparently expand in function effect annotations and effect set literals.

## Design
Reuse the transparent alias model. Effect set aliases expand during typechecking, participate in alias cycle detection, support Effect-kind parameters with existing `Task[E]` application syntax, and normalize through the existing effect set normalization rules. Checked and lowered IR should contain ordinary effect sets, not alias identities.

## Acceptance Criteria
- [x] `type UI = { Console, Log }` can be used as `! UI`.
- [x] `type Task[E : Effect] = { Coroutine, E }` can be applied in effect annotations.
- [x] Alias expansion detects cycles and reports source-oriented diagnostics.
- [x] `Task[Int]` or Type-kind aliases in effect position produce kind mismatch diagnostics.
- [x] Expanded effect sets are flattened, deduplicated, and order-insensitive according to existing effect equality.

## Relationships
- Depends on: ISS-074
- Parent: ISS-072
- Related: ISS-049
- Discovered from: ISS-072

## Notes
- 2026-07-03: Buslane lowering must remain alias-free.

## Close Notes
- 2026-07-03: Added kinded resolved effect alias arguments so Effect-kind alias parameters can receive effect arguments without resolver false positives. Implemented local effect alias expansion with Type and Effect parameter substitution, cycle detection, and kind mismatch diagnostics. Updated effect normalization to canonicalize unions. Added typecheck regressions for local aliases, generic Effect aliases, cycles, wrong-kinded arguments, and dedup/flatten behavior. Verified with `moon test modules/lanec/resolve`, `moon test modules/lanec/types`, `moon test modules/lanec/typecheck`, `moon info && moon fmt`, and `moon check --warn-list +73 --diagnostic-limit 200`.

# ISS-098: Move compiler diagnostic model into lanec diagnostic

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/diagnostics, area/compiler, refactor, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Create `modules/lanec/diagnostic` and move the compiler-facing diagnostic data model out of `modules/lanec/compile`. Rename `CompileDiagnostic*` to `CompilerDiagnostic*` so the model is not owned by the compile orchestration package and can also be used by formatting.

## Design
Add a new MoonBit package `Milky2018/lanec/diagnostic`. Move diagnostic structs and enums into it: `CompilerDiagnostic`, severity, labels, label style, facts, text edits, applicability, and suggestions. Update `lanec/compile` to import the new package and produce the renamed types. Do not add rendering or terminal behavior in this issue.

## Acceptance Criteria
- [x] `modules/lanec/diagnostic` exists with its own `moon.pkg`.
- [x] Compiler-facing diagnostic data types live in `modules/lanec/diagnostic`.
- [x] `CompileDiagnostic*` names are replaced by `CompilerDiagnostic*`.
- [x] `lanec/compile` produces `CompilerDiagnostic` values from the new package.
- [x] No terminal, LSP, or source rendering logic is added to `lanec/compile`.
- [x] `moon info && moon fmt`, `moon check --warn-list +73`, and targeted compiler tests pass.

## Relationships
- Depends on: none
- Parent: ISS-097
- Related: ISS-096
- Discovered from: ISS-096

## Notes
- 2026-07-04: This is the ownership move that makes later formatter and adapter work possible.

## Close Notes
Moved the compiler diagnostic data model into `Milky2018/lanec/diagnostic`, renamed the public model to `CompilerDiagnostic*`, and updated `lanec/compile`, `lane`, `lane/lsp`, and `lane_wasm` to consume the new package. `lanec/compile` no longer owns the diagnostic structs/enums. Verification: `moon check --warn-list +73`; `moon test modules/lanec/compile --warn-list +73`; `moon test modules/lane --warn-list +73`; `moon test modules/lane/lsp --warn-list +73`; `moon test modules/lane_wasm --warn-list +73`; `moon info`; `moon fmt`.

// invalid: nested enum payload coverage must still be exhaustive
module Examples.Invalid.NestedPatternNonExhaustive

enum Option[A] {
  none()
  some(A)
}

pub fn bad(value : Option[Option[I64]]) -> I64 {
  match value {
    none() => 0
    some(none()) => 1
  }
}

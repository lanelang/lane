# ISS-384: Give specialized data storage an explicit representation identity

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lanec, area/loisvm-lowering, area/bytecode, optimization, architecture, tests, agent
- Assignee: codex
- Created: 2026-08-18
- Updated: 2026-08-18
- External ref: modules/lanec/docs/adr/0136-cps-aware-callable-instance-planning.md

## Description
ISS-363 briefly used one exact Runtime ANF allocation site and closed runtime
arguments to specialize generic data storage. The resulting object shape was
still interned under the declaration's `DataFamilyId`. Multiple instantiations
could therefore emit different shapes for the same family and constructor tag,
causing duplicate-tag verifier failures, callable ABI mismatches, and runtime
traps. Allocation identity proves value flow; it does not create a nominal
representation identity.

The unsafe optimization has been removed. Generic constructors and matches once
again use their declaration-owned uniform erased ABI, leaving some CPS callable
adapters and erase/unerase bridges in otherwise closed paths.

## Design
If generic data storage is specialized, introduce an explicit lowerer-owned
representation family before object-shape interning. A family must cover the
complete constructor set and every construction, match, destruction, and
callable boundary that can carry its values. Its canonical key must preserve
the semantic runtime type expressions and evidence needed by nested payloads;
an outer `Reference` layout is insufficient.

The callable-instance plan remains the sole demand producer, but may only select
a specialized family whose complete closed use graph is proven. Open,
existential, or incompatible uses retain the declaration family, and the generic
implementation remains available. Do not reuse a declaration `DataFamilyId`,
guess identity from object shape, or choose storage while emitting bodies.

This is not required to complete ISS-363. Closed generated aggregates should be
scalar-replaced without changing their nominal storage ABI. ISS-368 now provides
the concrete workload: the Coroutine Scheduler retains and recursively forwards
existential `Task`, `Step`, `Runnable`, and `Queue` values, so scalar replacement
cannot preserve the closed answer-type equality across their escaping uses.

## Acceptance Criteria
- [x] Specialized and declaration-owned data families have distinct explicit identities before VM CFG emission.
- [x] One family identity owns exactly one complete constructor-tag and object-shape contract.
- [x] Constructor, match, destruction, callable adaptation, and bytecode verification consume the same family identity.
- [x] Nested runtime type expressions and higher-kinded evidence remain available when payload operations require them.
- [x] Open, existential, recursive-expanding, control-flow-joined, callable-valued, and multi-ABI uses retain the uniform declaration family.
- [x] Black-box regressions cover generic fallback plus specialized use, phantom higher-kinded constructors, and structural derivation over nested generic data.
- [x] A structured lowering observation demonstrates the intended erased-to-scalar bridge removal without duplicate shapes, runtime traps, or unused specialized families.
- [x] All valid examples pass under JIT and `--no-jit`.
- [x] `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, and `moon test --target native` pass.

## Relationships
- Depends on: none
- Parent: none
- Blocks: ISS-368
- Related: ISS-363, ISS-356
- Discovered from: ISS-363

## Notes
- 2026-08-18: Created after removing the allocation-local family experiment from ISS-363.
- 2026-08-18: Deferred as an independent future optimization. ISS-363 will use
  closed aggregate scalar replacement instead of specialized data storage.
- 2026-08-18: Reopened at priority 1 after the ISS-368 audit proved that the
  remaining Coroutine Scheduler adapters cross retained existential values.
  Treating the allocation-site answer type as the declaration family's ABI
  produced an interpreter `AbiMismatch` and Wasm traps; a distinct complete
  representation family is required before those adapters can be removed.
- 2026-08-18: Claimed for implementation. The representation-family contract
  will be produced by immutable planning before VM CFG emission; construction,
  matching, worker ABIs, and bytecode data-shape interning will consume that
  same identity. Emission-time family discovery and declaration-family reuse
  remain forbidden.

## Close Notes
- 2026-08-18: Completed with a lowerer-owned
  `DataRepresentationFamilyKey = Declaration | Specialized` catalog. Planning
  creates every family and its complete constructor storage contract before VM
  CFG emission; construction, matching, slot metadata, callable parameters and
  results, captures, object shapes, and bytecode verification consume that
  identity.
- 2026-08-18: Canonical keys retain runtime type expressions and
  higher-kinded evidence. The complete-use proof rejects open, existential,
  recursively expanding, joined, and incompatible flows. Callable-valued
  storage also retains the declaration family until ISS-385 makes callable ABI
  keys family-aware.
- 2026-08-18: The focused full-lowering regression simultaneously materializes
  the declaration `Option[T]` family with an erased payload and a distinct
  `Option[I64]` family with a direct scalar payload. Both own the complete
  `None`/`Some` tag set, bytecode verification accepts the image, and the
  interpreter observes `42`. Generic fallback and phantom higher-kinded cases
  are covered through the public compiler/backend matrix; real structural
  derivation examples cover nested generic data.
- 2026-08-18: The Coroutine Scheduler remains at 19 adapters and 28
  erase/unerase operations because its retained values cross existential and
  callable-valued storage boundaries. The earlier 8-adapter trial was unsound;
  it failed backend conformance. ISS-385 now owns the higher-order family-aware
  callable ABI required before ISS-368 can reduce those bridges.

# ISS-112: Improve diagnostics for generic expression application

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: area/diagnostics, area/typecheck, agent
- Assignee: codex
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Generic expression application diagnostics are currently adapted from type constructor and generic function inference diagnostics. For example, arity mismatches use a generic `generic expression` subject, and empty `[]` is reported as a generic parser unexpected-token error. These messages are correct enough, but they are not as precise as the language feature deserves.

## Design
Add diagnostic shapes and parser expectations that understand explicit generic expression application as a first-class construct. Error messages should identify the callee expression where possible, distinguish empty generic argument lists from unrelated parse failures, and explain Effect-kind vs Type-kind mismatches in terms of explicit generic arguments.

## Acceptance Criteria
- [x] Arity mismatch for `expr[Args]` reports a precise generic application subject and expected/actual argument counts.
- [x] Empty `expr[]` produces a targeted diagnostic explaining that empty generic argument lists are not allowed.
- [x] Kind mismatch for Effect-kind generic arguments points at the offending argument and explains the expected kind.
- [x] Non-generic callee diagnostics remain distinct from callability diagnostics.
- [x] CLI fixture tests lock the improved diagnostic output.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-063, ISS-107
- Discovered from: ISS-107

## Notes
- 2026-07-05: Discovered while adding invalid examples for `id[]`, `id[Int, Bool]`, `run[Int]`, and `plain[Int]`.

## Close Notes
- 2026-07-05: Added explicit generic-application diagnostic subjects, targeted empty generic/type parameter list parser errors, and regression coverage for arity, kind, and non-generic callee failures.
- Validation: `moon check --warn-list +73 --diagnostic-limit 200`; `moon test modules/lanec/parser --warn-list +73 --diagnostic-limit 200`; `moon test modules/lanec/typecheck --warn-list +73 --diagnostic-limit 200`; `moon test modules/lanec/compile --warn-list +73 --diagnostic-limit 200`; `moon test --warn-list +73 --diagnostic-limit 200`; `tools/check-lane-run-examples.sh`; `moon info`; `moon fmt`; `git diff --check`.

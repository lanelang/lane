# ISS-124: Adopt typed diagnostic ownership across compiler phases

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: area/diagnostics, area/compiler, architecture, refactor, agent
- Assignee: codex
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
Lane already has a shared compiler diagnostic data model and adapter, but diagnostic wording is still largely centralized in `modules/lanec/compile/diagnostic.mbt`. That makes compile orchestration understand parser, resolve, typecheck, link, artifact, and lint wording, and it keeps growing whenever a phase adds a new diagnostic.

Move toward the rustc-style model: diagnostics remain structured and uniformly rendered, while each compiler phase owns typed diagnostic conversion near the semantic data that produced the diagnostic.

## Design
Use handwritten MoonBit trait implementations instead of derive macros. `modules/lanec/diagnostic` should define the shared builder/context/trait surface. Parser, resolve, typecheck, and compile/link/artifact diagnostics should implement conversion near their owner packages. Do not introduce a global string registry.

Non-goals:
- Do not change diagnostic rendering or LSP transport semantics.
- Do not centralize all messages into one string table.
- Do not redesign typecheck, resolve, or parser diagnostic kinds.

## Acceptance Criteria
- [x] `lanec/diagnostic` exposes a small typed diagnostic conversion API usable by compiler phases.
- [x] Parser diagnostics no longer require `compile/diagnostic.mbt` to know parser wording.
- [x] Resolve diagnostics no longer require `compile/diagnostic.mbt` to know resolve wording.
- [x] Typecheck diagnostics no longer require `compile/diagnostic.mbt` to know typecheck wording.
- [x] Compile/link/artifact diagnostics remain owner-local and do not depend on source-language phase wording.
- [x] Golden, LSP, and example-smoke diagnostics remain stable or improve intentionally.
- [x] `compile/diagnostic.mbt` is reduced to orchestration glue or removed.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-063, ISS-096, ISS-097, ISS-098, ISS-100, ISS-119
- Discovered from: none

## Notes
- 2026-07-06: Created after comparing Lane's current layout with rustc's typed diagnostic model.

## Close Notes
2026-07-06: Completed through ISS-125, ISS-126, ISS-127, ISS-128, ISS-129, and ISS-130. Added the shared typed diagnostic conversion API, moved parser/resolve/typecheck conversions to owner packages, split compile/link/artifact diagnostics into owner-local files, and added a source-level architecture guard. Verification across the child issues included `moon info && moon fmt && moon check --warn-list +73`, `moon test --warn-list +73`, `moon test modules/lanec/compile --warn-list +73`, `moon test modules/lane --warn-list +73`, `moon test modules/lane/lsp --warn-list +73`, and `tools/check-lane-run-examples.sh`.

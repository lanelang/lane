# ISS-373: Consolidate compiler IR invariants and fact ownership

## Metadata
- Type: epic
- Status: open
- Priority: 1
- Labels: area/compiler, area/frontend, area/lanec, area/loisvm, area/wasm, architecture, refactor, tests, agent
- Assignee: unassigned
- Created: 2026-08-16
- Updated: 2026-08-26
- External ref: docs/review-standard.md

## Description
Lane has accumulated compiler representations whose public names imply stronger
phase boundaries than their structures enforce, and derived facts that are
copied or independently reconstructed by multiple packages. The resulting code
must synchronize parallel ASTs, side tables, index formulas, diagnostic
adapters, and observation catalogs even when one authoritative input already
contains enough information to derive the observation locally.

This epic makes the pipeline a chain of necessary, independently meaningful
representations. Every public IR must state and enforce its invariant, and every
authoritative fact must have one producer. An owner may derive and immediately
consume local observations without persisting another model. Transformation
time states, display snapshots, and cached projections do not become public IRs
merely because a debugger or Explore can observe them.

## Design
Retain the representations whose invariants are genuinely different: Syntax,
Resolved Source, Checked Source, Buslane, effect-aware CPS Core, Runtime ANF,
VM CFG, ARC-final VM CFG, physical-slot planning, LoisVM Bytecode, and Wasm.
Do not merge Buslane with VM CFG, ARC-final/physical-slot state, Bytecode, or
Wasm.

Deepen the Modules at the seams between those representations. Source
elaboration, module dependency analysis, effect lowering, Runtime ANF lowering,
execution-root planning, callable-adapter planning, Wasm identity allocation,
and Explore observation each own one coherent result. Consumers receive typed
facts; they do not recover meaning from names, traversal order, raw indices,
type spelling, or structural equality after lowering.

Replace old paths as each owner is established. Do not retain compatibility
fallbacks, parallel producers, public transitional ASTs, or duplicate white-box
tests. Prefer black-box pipeline coverage; use white-box tests only for
otherwise unobservable catalog completeness, canonical keys, and verifier
invariants.

## Acceptance Criteria
- [ ] Every retained public IR documents its invariant, sole construction or
      validation seam, verifier, and consumers in the owning glossary.
- [x] Source elaboration and module dependency diagnostics each have one
      producer shared by batch compilation and the semantic workspace.
- [x] Runtime ANF contains only backend-lowerable structure and runtime-relevant
      generic applications.
- [x] Effect-lowering time states are private implementation details rather
      than public IR seams.
- [ ] Executable initialization, callable-adaptation identity, Wasm
      function/table identity, and Explore stage/scale facts each have one
      authoritative producer without persistent duplicate projections.
- [x] Redundant source, module-object, entry, and external type side tables are
      removed, including the required artifact schema migration.
- [ ] ISS-370, ISS-372, and ISS-374 through ISS-383 are closed with their own
      verification evidence and no fallback implementations remain.
- [x] `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, and
      `moon test --target native` pass after the complete migration.

## Relationships
- Depends on: ISS-370, ISS-372, ISS-374, ISS-375, ISS-376, ISS-377, ISS-378, ISS-379, ISS-380, ISS-381, ISS-382, ISS-383, ISS-390
- Parent: none
- Related: ISS-297, ISS-344, ISS-356, ISS-363, ISS-364, ISS-368, ISS-369, ISS-371
- Discovered from: none

## Notes
- 2026-08-16: Created from a whole-pipeline ownership audit. The execution
  order is dependency-driven rather than issue-number-driven; Explore is
  intentionally last because it must consume the final stage and metric owners.

## Close Notes

- 2026-08-17: Closed after ISS-370, ISS-372, and ISS-374 through ISS-383 each
  established and verified its owning seam. The final Explore migration removed
  the parallel stage catalog and separated reconstructible structural analysis
  from transformation-owned provenance, so the epic no longer has an open
  implementation dependency.
- 2026-08-17: Final verification passed `moon info`, `moon fmt`,
  `moon check --target native --warn-list +73`, `moon test --target native`, and
  all 6 Lane Wasm package tests.
- 2026-08-26: Reopened after the representation architecture failed the
  deletion test. ISS-378's adapter recipe and ISS-385 through ISS-389's
  planner/graph/Physical-ANF stack persisted derived projections instead of
  deepening one representation-elaboration module. ADR-0138 and ISS-390 own
  the correction.

# LSP Documentation

This directory contains LSP-server design material.

- `adr/`: decisions relevant to native desktop LSP and diagnostics.
- `contexts/`: glossary slices for IDE tooling.

VS Code client behavior belongs in `lane_vscode`; compiler semantics belong in
`lanec`.

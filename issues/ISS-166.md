# ISS-166: Integrate effect erasure into LoisVM compilation

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: area/lanec, area/effects, area/loisvm, integration
- Assignee: codex
- Created: 2026-07-13
- Updated: 2026-07-13
- External ref: modules/lanec/loisvm_lowering/effect_erasure.mbt

## Description
Connect the completed effect-erasure passes to the production Lane-to-LoisVM path. The current lowering entry calls a primitive runtime-effect rewrite before ANF and rejects residual source handlers and resumes.

## Design
Run handler elaboration, `mon-trans`, `open-resolve`, and `monadic-lift` before ordinary ANF and VM CFG lowering. Lower the remaining compiler-private bind, handler-context, and continuation protocol into ordinary functions, calls, data, and closures. Retain a narrow final bridge that converts validated residual outer runtime operations into ordinary runtime-import calls using the existing synchronous primitive v1 ABI. Remove or rename the temporary `erase_effects_before_anf` implementation so the code and documentation do not imply that primitive rewriting is complete effect erasure.

## Acceptance Criteria
- [ ] `lower_linked_program_to_bytecode_with_runtime_effects` invokes handler elaboration and the three passes in canonical order.
- [ ] Source lexical handlers and resume expressions no longer reach the current rejection branches.
- [ ] Validated residual runtime operations lower to unified-function-table runtime imports.
- [ ] Compiler-private effect protocol operations become ordinary calls, data, and closures before VM CFG lowering.
- [ ] Unsupported host signatures still fail at the explicit primitive host ABI boundary.
- [ ] Existing pure, generic, closure, existential, ARC, artifact, and `lane run` tests remain passing.
- [ ] `moon info && moon fmt`, relevant package tests, and `git diff --check` pass.

## Relationships
- Depends on: ISS-165
- Parent: ISS-161
- Related: ISS-090, ISS-093, ISS-094
- Discovered from: ISS-161

## Notes
- 2026-07-13: This issue changes the production path but does not expand the v1 host ABI beyond synchronous primitive parameters and results.
- 2026-07-13: The selected effectful entry is preserved as the CPS source function and receives a generated pure wrapper whose lexical root handlers call synthetic runtime imports and resume normally.

## Close Notes

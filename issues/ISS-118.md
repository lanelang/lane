# ISS-118: Warn on unused imports and opens

## Metadata
- Type: feature
- Status: closed
- Priority: 2
- Labels: area/imports, area/typecheck, area/diagnostics, area/lint, agent
- Assignee: codex
- Created: 2026-07-05
- Updated: 2026-07-07
- External ref: none

## Description
Lane should emit a warning when an imported module or open exposure is not used by the module being compiled.

## Design
Base the warning on resolved name usage and import/open exposure records rather than raw text. Imports used only for type names, effect names, constructors, operations, or qualified value references must count as used. Open exposure should warn only when it contributes no selected name to resolution. This issue should stay separate from missing-import errors and module-input validation.

## Acceptance Criteria
- [x] Completely unused imports produce warning diagnostics at import locations.
- [x] Imports used through values, types, effects, constructors, operations, or qualified references do not warn.
- [x] Open exposures that contribute no resolved names produce warning diagnostics.
- [x] Open exposures that contribute at least one resolved name do not warn.
- [x] The behavior is locked by focused tests and CLI/LSP diagnostic fixtures.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-115, ISS-116, ISS-117
- Discovered from: none

## Notes
- 2026-07-05: Created separately from unused declaration checks because import/open usage depends on name-resolution provenance.
- 2026-07-07: Started implementation. The fix should add resolver-level import exposure/use provenance instead of inferring usage from rendered names after resolution.

## Close Notes
- 2026-07-07: Implemented W2004 using resolver-level import exposure/use provenance plus checked-AST symbol usage for imported type/effect metadata. This keeps explicit name uses, qualified module uses, selective import items, open-import contributions, and metadata-only imports such as field access through imported interface types structurally visible to diagnostics. Added compile wbtests and a warnings example fixture.

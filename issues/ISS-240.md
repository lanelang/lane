# ISS-240: Separate coverage witnesses from checked source patterns

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: area/typecheck, area/patterns, diagnostics, refactor, agent
- Assignee: unassigned
- Created: 2026-07-21
- Updated: 2026-07-31
- External ref: modules/lanec/typecheck/pattern.mbt

## Description
Pattern exhaustiveness witness generation constructs synthetic `CheckedPattern` trees and fills every source location with `SourceSpan::dummy()`. This reuses a source-bearing semantic AST for generated coverage terms, allowing synthetic locations to leak into later logic and obscuring which pattern operations require real source provenance. Missing-witness rendering also embeds an unexplained recursion depth of six.

## Design
Give the coverage algorithm a private, span-free pattern or constructor representation designed for matrix usefulness and witness rendering. Convert source `CheckedPattern` values into that representation at the coverage seam instead of manufacturing checked source nodes. Name and document the witness-expansion budget; reaching it may abbreviate presentation but must not change exhaustiveness correctness.

## Acceptance Criteria
- [x] Coverage candidate generation does not construct `CheckedPattern` values with dummy spans.
- [x] Coverage usefulness and witness rendering use a private span-free representation.
- [x] The witness-expansion budget is named, documented, and tested on deeply nested nominal types.
- [x] Exhaustiveness and redundancy decisions are unchanged when witness rendering reaches its budget.
- [x] User-facing missing-pattern diagnostics remain readable and source-located at the owning match or declaration.
- [x] `moon info`, `moon fmt`, focused tests, full tests, and `git diff --check` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-07-21: `coverage_candidates` currently creates literals, variants, structs, fields, and wildcards with `SourceSpan::dummy()` solely to feed the pattern matrix.

## Close Notes

- Closed on 2026-07-31 after introducing the private span-free `CoveragePattern` representation and converting checked source patterns only when they enter the pattern matrix.
- Candidate generation, matrix specialization/usefulness, wildcards, and missing-witness search now use coverage-owned patterns; `pattern.mbt` no longer constructs any `CheckedPattern` with `SourceSpan::dummy()`.
- The documented `witness_expansion_budget` abbreviates only presentation. Deep recursive-enum regressions freeze the abbreviated witness and independently prove exhaustive/redundant-arm decisions beyond the budget.
- Verification passed: pattern tests 30/30, typecheck tests 243/243, `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, `moon test --target native` (1321/1321), and `git diff --check`.

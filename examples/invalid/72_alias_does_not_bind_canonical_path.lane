// An aliased qualified import binds only its alias, not the canonical path.
module Examples.Invalid.AliasDoesNotBindCanonicalPath

import Basic.Data.Option as Maybe

fn invalid() -> Basic.Data.Option.Option[I64] {
  Maybe.Option::some(42)
}

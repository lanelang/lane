module Examples.Valid11
pub fn apply(value : Int, f : (Int) -> Int) -> Int {
  f(value)
}

fn[A] apply_generic(value : A, f : (A) -> A) -> A {
  f(value)
}

pub let id_int : (Int) -> Int = fn(value) {
  value
}

pub let id_poly : [A](A) -> A = fn[A](value) {
  value
}

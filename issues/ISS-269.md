# ISS-269: Deepen the public compiler facade

## Metadata
- Type: task
- Status: open
- Priority: 2
- Labels: area/compile, public-api, architecture, model, refactor, migration, agent
- Assignee: unassigned
- Created: 2026-07-31
- Updated: 2026-07-31
- External ref: modules/lanec/compile/compile.mbt, modules/lanec/compile/pkg.generated.mbti, modules/lane/moon.pkg

## Description
The public compiler facade exposes result records built from optional values
plus diagnostics and leaks source, module-compilation, link, and lowering types
from internal packages. Those records admit invalid combinations and the facade
does not provide enough leverage for tools to avoid importing pipeline
packages directly.

## Design
Define a closed compilation outcome that distinguishes successful values plus
warnings from failures plus errors. Give the facade ownership of its public
source-set and request types. Consolidate redundant text/input wrappers around
one canonical implementation while retaining only convenience entrypoints that
materially improve ordinary use.

Migrate CLI, LSP, Wasm, and library consumers to stable `compile`, `artifact`,
and `driver` boundaries. Do not preserve old internal-type signatures through
compatibility wrappers solely to avoid caller migration.

## Acceptance Criteria
- [ ] Public check, compile, module-compile, and link results cannot represent success/failure combinations forbidden by the compiler contract.
- [ ] The facade owns the public request/input types required by ordinary compiler consumers.
- [ ] Public signatures do not expose incidental `module/frontend`, `module/compile`, or `module/link` types without an explicit boundary reason.
- [ ] Redundant wrapper entrypoints are removed or justified by distinct user workflows.
- [ ] Lane CLI, LSP, and Wasm callers use stable facade, artifact, or driver APIs where applicable.
- [ ] Black-box tests cover public success-with-warnings, parse/resolve/type failures, module compilation, and linking outcomes.
- [ ] `moon info`, `moon fmt`, focused tests, `moon test --target native`, examples, and `git diff --check` pass.

## Relationships
- Depends on: ISS-266, ISS-268
- Parent: none
- Related: ISS-148, ISS-149, ISS-188
- Discovered from: none

## Notes
- 2026-08-01: Status corrected from `in_progress` to `open`. No acceptance
  criterion is met, there is no work in flight, and the state was blocking
  ISS-272 and ISS-274 behind something nobody was doing.
- 2026-07-31: The current generated interface exposes `SourceInput`, `CompiledModuleSet`, `LinkResult`, and Buslane side-table types through the facade.

## Close Notes

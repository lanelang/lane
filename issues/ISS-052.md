# ISS-052: Validate Lane effect lowering end to end

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lanec, area/buslane, area/effects, tests, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-02
- External ref: modules/buslane/docs/roadmap-checklist.md

## Description
After metadata, latent effects, operation calls, and handlers lower into
Buslane, the integration needs a final validation pass before ISS-037 closes.

## Design
Add or audit end-to-end coverage from Lane source through checked source into
Buslane effect core. The validation should include effect metadata, latent
function effects, operation calls, handlers, residual effects, and no source
syntax leaking into Buslane output.

## Acceptance Criteria
- [x] Integration snapshots cover effect metadata lowering.
- [x] Integration snapshots cover latent function effects.
- [x] Integration snapshots cover operation-call lowering to `perform`.
- [x] Integration snapshots cover handler lowering to Buslane handler tables.
- [x] Buslane verifier accepts the lowered valid effect examples.
- [x] No source spans or source-only payload patterns appear in Buslane nodes.
- [x] `moon test modules/lanec/elaborate --warn-list +73` passes.
- [x] `moon check modules/lanec --warn-list +73` passes.
- [x] `moon test --warn-list +73` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: ISS-051
- Parent: ISS-037
- Related: ISS-038
- Discovered from: ISS-037

## Notes
- 2026-07-01: Final ISS-037 validation gate. Split newly discovered unrelated
  gaps instead of expanding this issue.

## Close Notes
Closed after auditing the focused Lane-to-Buslane effect lowering snapshots and
running the validation gate. Existing elaborate snapshots now cover effect
metadata, latent function effects including row variables, operation-call
lowering to `perform`, and handler lowering to Buslane handler tables. The
valid lowered examples are accepted by `@buslane.verify_program`, and the
Buslane snapshots show Buslane metadata, `perform`, and positional handler
payload binders rather than Lane source spans or source payload-pattern nodes.

Verification:
- `moon test modules/lanec/elaborate --target native --warn-list +73`
- `moon check modules/lanec --warn-list +73`
- `moon test --warn-list +73`
- `moon info && moon fmt`
- `git diff --check`

# ISS-031: Add Buslane effect identities and effect terms

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/buslane, area/effects, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: modules/buslane/docs/roadmap-checklist.md

## Description
Buslane needs first-class effect metadata before `perform`, handlers, verifier
rules, or interpreter behavior can be implemented. Add the core identity and
type-level representation needed by the effect roadmap.

## Design
Introduce `EffectId` and `OperationId`, effect constructor metadata, operation
signature metadata, `Kind::Effect`, and effect terms for empty effects,
singleton effects, effect row variables, and unions. Reuse `TypeParameterId`
with `Kind::Effect` for effect row variables; do not introduce a separate
runtime identity family.

## Acceptance Criteria
- [x] Buslane metadata can represent effect constructors and operation
  signatures.
- [x] Buslane kinds include `Effect`.
- [x] Buslane type-level effect terms represent empty, singleton, row-variable,
  and union forms.
- [x] Effect row variables use `TypeParameterId` with `Kind::Effect`.
- [x] `moon info && moon fmt` has been run after the change.
- [x] Relevant Buslane model tests or snapshots cover the new metadata.

## Relationships
- Depends on: none
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-07-01: Split from the Buslane effect roadmap. This is the root task for
  the implementation chain.

## Close Notes
- 2026-07-01: Added `EffectId`, `OperationId`, `EffectInfo`,
  `OperationInfo`, `Kind::Effect`, and the `Effect` term model to
  `modules/buslane`. Added define/lookup helpers for effect constructors and
  operations, pretty output for effect metadata and effect terms, and a
  whitebox model test covering effect operation metadata. Updated Buslane text
  kind support for `Effect` so existing text import/export remains compatible
  with the extended kind set. Verified with `moon check modules/buslane
  --warn-list +73`, `moon check modules/buslane/text --warn-list +73`, `moon
  test modules/buslane --warn-list +73`, `moon test modules/buslane/text
  --warn-list +73`, and `moon info && moon fmt`.

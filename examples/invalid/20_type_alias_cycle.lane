module Examples.Invalid20
// Invalid: transparent top-level type aliases must form an acyclic dependency
// graph. Expanding A reaches B, which reaches A again.

pub type A = B
pub type B = A

pub let answer : A = 42

# ISS-293: Give a CPS'd callable one owner for its binders

## Metadata
- Type: task
- Status: open
- Priority: 0
- Labels: area/effect-lowering, area/cps, correctness, architecture, agent
- Assignee: unassigned
- Created: 2026-08-01
- Updated: 2026-08-01
- External ref: modules/lanec/effect_lowering/cps/context_schema.mbt, modules/lanec/effect_lowering/cps/abi.mbt

## Description
`DictionaryCpsSchema::rewrite_type` allocates a fresh `%answer` and `%residual`
every time it meets an effectful function type. Nothing ties one allocation to
another, so the same source type rewritten twice yields two unrelated pairs, and
a declared type assembled from one rewrite can end up beside an implementation
assembled from another.

`examples/valid/45_async_effect.lane` is the measured case. A CPS'd callable's
committed type binds one pair while its `%context` and `%continuation`
parameter values are typed at a second, leaving that second pair free:

```
Forall([%answer#404, %residual#405],
  Function([ Schedule%dictionary<%answer#414, %residual#415>, ... ],
           Parameter(%answer#404), Parameter(%residual#405)))
```

The uniques are allocated in order, so the binders and the parameter values
come from different generations of the same source type.

`commit_cps_binding_type` makes this reachable rather than catching it. It sets
a declaration's type from the type synthesized out of the lowered expression, so
metadata follows the implementation wherever the implementation went, and the
two cannot be compared against a common intent.

## Design
A function occurrence gets one shape, created once, describing everything the
CPS'd form needs:

```
CpsCallableShape
  source_type
  target_type
  answer_parameter
  residual_parameter
  context_parameter_types
  value_parameter_types
  result_type
```

Both consumers read the same shape: the declaration's committed type, and the
`TypeLambda` plus `Function` that implements it. A nested function is driven by
the shape its consuming position supplies; where there is no expected type, the
source type is synthesized once and one shape is created from it. Metadata and
AST are never derived separately.

`commit_cps_binding_type` goes away. The shape is the intent, and the
implementation is checked against it rather than defining it:

```
type_is_consumable_as(synthesize(lowered_rhs), committed_type)
```

Consumability rather than equality, because a lowered body's row is legitimately
narrower than the row its position declares — that is the result ISS-284
established, and asserting equality here would fire on correct output.

Checking at each binding is the point. A divergence introduced while lowering
one function is reported there, naming that function, instead of surfacing after
monadic-lift as a `TypeMismatch` between two large types with no indication of
which pass produced either.

A structural cache on `rewrite_type` is not a substitute and must not be added.
Sharing a binder identity between two quantifier positions is the defect, not
the fix.

## Acceptance Criteria
- [ ] One `CpsCallableShape` per function occurrence feeds both the declared
      type and the implementation.
- [ ] `commit_cps_binding_type` is replaced by a consumability assertion against
      the committed type, checked as each binding is lowered.
- [ ] A CPS focused test covers a higher-order effectful parameter, a nested
      function, and a context dictionary sharing one shape, and fails on a free
      binder rather than accepting two alpha-equivalent types.
- [ ] `examples/valid/45_async_effect.lane` verifies after monadic-lift.
- [ ] `moon check`, `moon test --target native`, and
      `tools/check-lane-run-examples.sh` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-292, ISS-283
- Discovered from: ISS-284

## Notes
- 2026-08-01: The divergence is measured but its origin is not yet identified.
  `lower_cps_function` takes its binders from `expected_cps_type` and defines
  its parameter values from those same binders, so it cannot diverge on that
  path. Confirm which site does before building the shape, so the refactor is
  validated against a known target rather than assumed to cover it.

## Close Notes
Leave empty until closing.

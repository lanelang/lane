# ISS-105: Apply width-sensitive formatting to declarations and expression heads

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/formatter, area/lanec, area/syntax, area/tests, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: modules/lanec/docs/adr/0059-width-sensitive-source-formatting.md

## Description
Convert declaration heads and expression chains to use width-sensitive formatting after list-like syntax is covered.

## Design
Cover `let` and `offer` initializers, function and effect operation signatures, function literals, `if` heads, `match` heads, `handle` heads, binary operator chains, pipeline chains, and field-access chains. Broken assignments move the initializer to the next line with two-space indentation. Broken binary, pipeline, and field-access continuations put the operator, `|>`, or `.` at the start of the continuation line.

## Acceptance Criteria
- [x] Long `let` and `offer` initializers format with the initializer on a continuation line.
- [x] Function, function literal, and effect operation signatures prefer broken parameter lists before breaking return/effect tails.
- [x] Long `if`, `match`, and `handle` heads break before their controlling expression without losing structural clarity.
- [x] Binary, pipeline, and field-access chains use operator-leading continuations when broken.
- [x] Formatter output remains parseable and idempotent for each covered shape.
- [x] `moon test modules/lanec/syntax --warn-list +73` and `moon test modules/lanec/format --warn-list +73` pass.

## Relationships
- Depends on: ISS-104
- Parent: ISS-102
- Related: none
- Discovered from: none

## Notes
- 2026-07-04: This task touches precedence-sensitive expression layout; keep regression tests focused and broad enough to catch parse drift.

## Close Notes
Added assignment continuation layout for `let`, `offer`, and pattern lets; suffix-aware function and effect operation signatures; breakable `if`, `match`, and `handle` heads; operator-leading binary expressions; flattened pipeline chains; and dot-leading field-access chains. Verification: `moon test modules/lanec/syntax --warn-list +73`; `moon test modules/lanec/format --warn-list +73`.

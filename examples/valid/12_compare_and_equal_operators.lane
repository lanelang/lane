module Examples.Valid12

import Stdlib.Io.{Io, Write}

import Stdlib.Ops.*

pub fn compare_ints(a : Int, b : Int) -> Bool {
  if a < b {
    a != b
  } else {
    a == b || a > b
  }
}

pub fn answer() -> Unit ! Io {
  if compare_ints(1, 2) && compare_ints(2, 1) {
    Write::println!("compare=true")
  } else {
    Write::println!("bad:compare=true")
  }
}

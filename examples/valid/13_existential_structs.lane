module Examples.Valid13

import Basic.Io.{ println }

pub struct Hide {
  type T
  val : T
}

pub fn repack(h : Hide) -> Hide {
  {
    let Hide::{ T, val } = h
    Hide::{ T = T, val }
  }
}

pub let packed_i64 : Hide = Hide::{ T = I64, val: 42 }

pub let repacked_i64 : Hide = repack(packed_i64)

pub fn answer() -> Unit ! Io {
  println("existential struct packed")
}

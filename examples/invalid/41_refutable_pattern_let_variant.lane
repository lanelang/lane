module Examples.Invalid.RefutablePatternLetVariant

enum Option[A] {
  none()
  some(A)
}

fn bad(value : Option[I64]) -> I64 {
  {
    let some(x) = value
    x
  }
}

let value : I64 = bad(Option::none())

# ISS-241: Deepen completion cursor classification

## Metadata
- Type: task
- Status: closed
- Priority: 3
- Labels: area/analysis, area/completion, area/parser, lsp, refactor, agent
- Assignee: codex
- Created: 2026-07-21
- Updated: 2026-08-08
- External ref: modules/lanec/analysis/completion.mbt

## Description
Completion candidates now come from the unified semantic index, but cursor context is still classified by a collection of backward token scans and source-string offset helpers. The classifier manually recognizes imports, owner separators, type aliases, type/effect positions, and identifier prefixes. Every new syntax form can require another branch in completion independently of parser semantics.

## Design
Create one internal completion-syntax context module that accepts lexer tokens plus a cursor and returns a structured context such as import path, owner-qualified member, receiver selector, type position, effect position, or value position. Reuse parser-owned token and source-position utilities where possible. The module must remain tolerant of incomplete source; do not require a successfully parsed AST and do not move semantic candidate production back out of `SemanticIndex`.

## Acceptance Criteria
- [x] Completion context classification has one interface and no scattered backward grammar scans in candidate production.
- [x] Import, owner, receiver, type, effect, and value contexts are represented structurally.
- [x] Identifier prefix and replacement spans use shared source-position utilities with UTF-8 behavior documented by tests.
- [x] Incomplete and syntactically invalid source continues to receive useful completions.
- [x] Adding a new declaration form does not require parallel completion-space heuristics outside the classifier.
- [x] `moon info`, `moon fmt`, completion/LSP tests, full tests, and `git diff --check` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-238
- Discovered from: none

## Notes
- 2026-07-21: Current helpers include `completion_context`, `completion_owner_before_separator`, `completion_space`, `completion_declaration_is_type_alias`, and local line/column conversion routines.

## Close Notes

- 2026-08-08: Moved incomplete-source cursor classification, token ownership scans, prefix extraction, and source-position conversion behind the single `completion_context` seam in `completion_syntax.mbt`; semantic candidate production now consumes only the resulting structured context.
- 2026-08-08: Preserved structural import, owner, receiver, type, effect, value, and pattern contexts and added a regression documenting one-based columns over decoded UTF-8 scalar values.
- 2026-08-08: `moon info`, `moon fmt`, focused analysis/LSP tests, all 1579 native tests, and `git diff --check` pass.

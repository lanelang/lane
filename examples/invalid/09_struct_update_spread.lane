// invalid: struct spread and copy update are not supported in v1
module Examples.Invalid09

pub struct Point {
  x : I64
  y : I64
}

pub let point : Point = Point::{ x: 1, y: 2 }
pub let moved : Point = Point::{ ..point, x: 3 }

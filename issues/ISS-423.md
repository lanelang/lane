# ISS-423: Direct-call exact packed callable targets

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: area/vmcfg, area/physical, area/wasm, optimization, callable-flow, agent
- Assignee: codex
- Created: 2026-08-29
- Updated: 2026-08-29
- External ref: modules/lanec/docs/optimization-priorities.md

## Description
Callable-flow proves that some final indirect calls have exactly one physical
target, but devirtualization retains `CallValue` when the environment is known
only as `PackedEnvironment`. Current Explore baselines contain 35 such calls in
the pinned Basic test entry and nine in the coroutine scheduler. They still pay
indirect-table dispatch and callable ABI guards even though target selection is
no longer dynamic.

The packed callable itself may remain semantically necessary because it escapes
through aggregate storage or another first-class boundary. The removable fact
is only the call-site target dispatch; this task must not claim that every
corresponding closure or environment can be deleted.

## Design
`CallableFlowAnalysis` remains the sole producer of target and environment
knowledge. Extend VM CFG devirtualization with one explicit representation for
a direct target whose environment is borrowed from a still-live packed callable.
The operation must preserve the packed callable as the environment owner for
the complete call and use the target's verified callable ABI. Physical
finalization and Wasm emission consume this form; they do not rediscover the
target by inspecting callable bits or table entries. Wasm authenticates that
the packed carrier's target field equals the explicit target before extracting
the environment. This check does not select a target or repeat callable ABI
classification; it preserves the Physical IR trust boundary for independently
constructed programs.

Rewrite only `ExactTarget(target, Packed)` facts with a complete ABI match.
Multiple and unknown targets remain indirect. Do not use call frequency,
function size, source provenance, or a profitability threshold. Function-table
reachability consumes the rewritten direct edge through its existing owner.
Environment ABI planning runs exactly once: changing a callee ABI is an
irreversible program transformation and cannot become a new planning premise.
Moving a single-use direct body can expose another exact callsite, so a second,
callsite-only devirtualization step runs to a structural fixed point after
move-inlining. It may rewrite calls using the already established ABI, but it
must not produce new environment ABI plans. Termination is defined by unchanged
VM CFG, not an iteration budget.

## Acceptance Criteria
- [x] VM CFG represents a direct call that borrows its environment from an
      owned or borrowed packed callable without shortening the owner's lifetime.
- [x] Callable-flow is the only producer deciding when an exact packed target
      can use that representation.
- [x] Physical and Wasm lowering consume the direct edge without an indirect
      callable ABI guard or target-table dispatch.
- [x] Capture-free, local-environment, packed-environment, multiple-target, tail
      call, borrowed-callable, and fatal-termination cases have focused regressions.
- [x] The coroutine scheduler removes its nine exact-target packed indirect
      calls and preserves output.
- [x] The pinned Basic entry removes its 35 exact-target packed indirect calls
      and preserves output.
- [x] Explore records the resulting direct/indirect, runtime-guard, table, and
      Wasm instruction changes without a heuristic budget.
- [x] `moon info`, `moon fmt`, strict native check, native tests, Basic tests,
      and valid examples pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-369, ISS-378, ISS-395, ISS-422
- Discovered from: ISS-422

## Notes
- 2026-08-29: Created from the current Protocol v11 expansion baseline. Exact
  packed-target counts are owner-produced callable-flow observations, not a WAT
  pattern count.

## Close Notes
- 2026-08-29: Added explicit packed-direct normal and tail calls to VM CFG and
  Physical IR. Callable-flow alone selects the target; ARC preserves the packed
  callable as the transferred environment owner, while Physical verification
  checks its full callable shape and target ABI.
- 2026-08-29: Wasm emits a direct call after authenticating the carrier target
  with one shared helper. Focused tests cover stored/borrowed callables, tail
  calls, mismatched carriers, fatal termination, and invalid Physical input.
- 2026-08-29: Environment ABI planning remains single-shot. A separate
  callsite-only fixed point handles calls exposed by single-use move-inlining;
  repeating ABI planning was rejected because it made rewritten callee ABIs
  into unsound new planning premises.
- 2026-08-29: Scheduler direct/indirect calls changed from 22/43 to 31/34 and
  Wasm instructions from 7,140 to 7,128. Basic changed from 451/148 to 479/120
  and from 61,928 to 61,835. Both exact-packed residual counts are zero and
  callable tables remain 80/363 because the carriers still escape.
- 2026-08-29: `moon info`, `moon fmt`, strict native check, all 1,729 native
  tests, all 12 Basic test groups, and all valid examples passed.

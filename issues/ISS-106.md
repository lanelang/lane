# ISS-106: Refresh examples and complete formatter validation

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/formatter, area/examples, area/tests, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: modules/lanec/docs/adr/0059-width-sensitive-source-formatting.md

## Description
Run the completed width-sensitive formatter through examples, fixture updates, and full project validation.

## Design
After syntax pretty-printing is updated, format Lane examples using a deterministic file list and update only intentional formatting changes. Review diffs for readability rather than blindly accepting churn. Then run the full validation gates required for formatter work.

## Acceptance Criteria
- [x] Lane examples are formatted with the new formatter behavior where applicable.
- [x] Any fixture changes are reviewed and intentional.
- [x] `moon check --warn-list +73` passes.
- [x] `moon test modules/lanec/syntax --warn-list +73` passes.
- [x] `moon test modules/lanec/format --warn-list +73` passes.
- [x] `moon test --warn-list +73` passes.
- [x] `tools/check-lane-run-examples.sh` passes.
- [x] `moon fmt && moon info` produces only expected diffs.
- [x] `git diff --check` is clean.

## Relationships
- Depends on: ISS-105
- Parent: ISS-102
- Related: none
- Discovered from: none

## Notes
- 2026-07-04: Use a deterministic source list such as `rg --files examples -g '*.lane'` if examples need to be reformatted.

## Close Notes
Formatted parseable Lane examples with the new formatter behavior and updated the parser pretty snapshot for pipeline continuation. `moon info` produced no `.mbti` changes. Verification: `moon check --warn-list +73`; `moon test modules/lanec/syntax --warn-list +73`; `moon test modules/lanec/format --warn-list +73`; `moon test --warn-list +73`; `tools/check-lane-run-examples.sh`; `moon fmt && moon info`; `git diff --check`.

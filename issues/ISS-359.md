# ISS-359: Preserve function provenance through canonical bytecode lowering

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/loisvm-lowering, area/vmcfg, provenance, canonicalization, tests, agent
- Assignee: codex
- Created: 2026-08-11
- Updated: 2026-08-11
- External ref: modules/lanec/loisvm_lowering/function_table.mbt

## Description
LoisVM function-table canonicalization reorders bodies and merges duplicate
runtime imports. A provenance array created before this boundary would drift
unless the canonicalization owner remaps and merges it with the image.

## Design
Canonicalization produces one product containing the canonical VM CFG image and
its canonical function provenance. VM CFG finalization preserves FunctionId
order and passes the sidecar unchanged to the final bytecode observation. The
persisted bytecode image remains free of exploration metadata.

## Acceptance Criteria
- [x] Canonicalized VM CFG functions and origins have identical cardinality and
      FunctionId indexing.
- [x] Runtime import sorting and deduplication preserve all relevant origins.
- [x] Final bytecode functions retain the canonical VM CFG provenance exactly.
- [x] No artifact or bytecode schema change is introduced.
- [x] Tests cover reordering, duplicate imports, bodies, and failure paths.

## Relationships
- Depends on: ISS-358
- Parent: ISS-356
- Related: ISS-274, ISS-344
- Discovered from: ISS-356

## Notes
- 2026-08-11: The canonicalization boundary is the sole owner of FunctionId
  remapping and must not expose a second mapping algorithm to Explore.
- 2026-08-11: Claimed after ISS-358 established typed origins and atomic
  function-plan allocation.
- 2026-08-11: Function-table canonicalization now returns the canonical VM CFG
  image and provenance as one product. Body origins follow body order; sorted,
  deduplicated runtime imports merge every source origin. The final bytecode
  observer receives the same sidecar after VM CFG finalization.

## Close Notes
- Added structured compiler-defect failures for cardinality drift, invalid
  FunctionId references, missing mappings, and finalization count drift.
- The persisted bytecode and artifact models are unchanged.
- The native suite passes with regressions for body/import ordering, duplicate
  import origin merging, VM CFG-to-bytecode preservation, and failure paths.

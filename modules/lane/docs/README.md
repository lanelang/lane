# Lane Command Documentation

This directory contains user-facing tool design material.

- `adr/`: decisions for entry selection, runtime errors, environment model,
  LSP deployment, and v1 execution constraints.
- `contexts/`: glossary slices relevant to running Lane programs and editor
  tooling.

Compiler implementation details belong in `lanec`; language rules belong in
`spec`.

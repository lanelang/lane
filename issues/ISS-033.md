# ISS-033: Add Buslane perform and handler syntax to text roundtrip

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/buslane, area/effects, area/text, tests, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: modules/buslane/docs/roadmap-checklist.md

## Description
Buslane text import/export must be able to represent effect-core programs so
fixtures, snapshots, and future Lane IR explorer tooling can roundtrip them.

## Design
Add Buslane expression forms for `perform`, handler tables, operation
alternatives, and resume values, then update pretty printing and text parsing.
Handler tables remain grouped by singleton effect before operation
alternatives. Operation alternatives bind evaluated payload values
positionally; source payload patterns are lowered before Buslane.

## Acceptance Criteria
- [x] Buslane expressions include `perform`, handler tables, operation
  alternatives, and resume values.
- [x] Pretty output covers effect metadata, function latent effects, `perform`,
  handlers, and resume values.
- [x] Text parsing and import/export support all effect-core forms.
- [x] Roundtrip tests cover pure function types, open effect rows, `perform`,
  and deep handlers.
- [x] `moon test modules/buslane/text` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: ISS-031, ISS-032
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-07-01: Split from the Buslane effect roadmap.

## Close Notes
- 2026-07-01: Added Buslane AST nodes for `Perform`, `Handle`, `Resume`,
  `HandlerTable`, and `OperationAlternative`. Added human pretty output and
  canonical text import/export for effect and operation metadata, effect terms,
  `perform`, handler tables, operation alternatives, and resume values.
  Updated verifier, interpreter, module-object remapping, and Buslane-to-ANF
  lowering exhaustiveness without implementing the later effect judgment or
  deep-handler runtime semantics. Added text roundtrip coverage for open effect
  rows, `perform`, and a deep handler shape. Verified with `moon test
  modules/buslane/text --warn-list +73`, `moon test modules/buslane --warn-list
  +73`, `moon check modules/lanec/compile --warn-list +73`, `moon check
  modules/lanec/anf --warn-list +73`, and `moon info && moon fmt`.

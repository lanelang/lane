# ISS-195: Validate and close the LoisVM executable migration

## Metadata
- Type: task
- Status: blocked
- Priority: 0
- Labels: area/lanec, area/loisvm, tests, examples, migration
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: tools/check-lane-run-examples.sh

## Description
Run the complete project and example validation gates, fix migration regressions, and close the whole-program LoisVM executable epic with reproducible evidence.

## Design
Start with focused package tests, then run generated-interface and formatting gates, all-target checks and tests, compile/link smoke, and the authoritative example script. Fix only defects caused by this migration and record unrelated failures separately.

## Acceptance Criteria
- [ ] `moon info && moon fmt` produce only expected changes.
- [ ] `moon check --target all --warn-list +73` and `moon test` pass.
- [ ] `tools/check-lane-run-examples.sh` passes all valid run examples and diagnostic fixtures.
- [ ] The epic and all child issues contain concrete close notes and regenerated tracker index.

## Relationships
- Depends on: ISS-194, ISS-196
- Parent: ISS-186
- Related: ISS-001, ISS-002, ISS-003
- Discovered from: ISS-186

## Notes

- 2026-07-14: Claimed after `fd90e90` completed command and artifact integration; beginning full validation and migration regression fixes.
- 2026-07-14: Fixed function-valued data fields in executable initializers by materializing adapted constructor payloads; the lowering package has 29/29 native tests and example 01 runs successfully.
- 2026-07-14: Added owned-value liveness threading before ARC insertion so branch-local uses receive explicit block owners and untaken paths release them; vmcfg has 10/10 native tests and example 01 prints `42`.
- 2026-07-14: Implemented ADR-0113 scalar externals as zero-argument primitive runtime imports invoked by the initializer; lowering has 30/30 native tests and all-target warning-enabled checks pass.
- 2026-07-14: Updated executable examples that intentionally had no host binding: builtin expected-type coverage remains in unreachable functions, while existential runtime examples use concrete integers. Examples 07, 13, 14, and 42 now run successfully.
- 2026-07-14: Preserved selective-CPS effect companion identities through executable elaboration and LoisVM lowering, flattened staged `forall` applications, propagated effect substitutions through nominal types, and kept concrete callable types while building erased adapters. Focused effect/CPS/executable/lowering tests pass 59/59; examples 40 and 45 print `42`. Example 37 now lowers but still exposes a deeper CPS answer-type callable ABI mismatch during execution.
- 2026-07-14: Commit `06be8dc` makes staged type substitution transitively resolve parameters inside replacement callable types; its focused regression test was observed failing before the fix and passing afterward. Example 41 then isolated the remaining issue to answer-dependent effect companion contexts rather than ordinary type substitution.
- 2026-07-14: Commit `456a173` preserves a returned effectful callable's own CPS Answer through eta-expansion, recognizes compiler-generated CPS application suffixes while source metadata is still present, and permits callable adapters to omit only target parameters proven to specialize to Unit. Effect-lowering tests pass 20/20, CPS tests pass 23/23, LoisVM lowering tests pass 36/36, and examples 40, 41, and 45 print `42`.
- 2026-07-14: Example 37 still traps with `indirect call type mismatch`. Interpreter/VM CFG instrumentation proved that `run_task[E]` executes a handled Coroutine clause under local answer `M_E<Step>`, but the abstract residual context `E` is the opaque context tied to the outer final answer. The clause therefore returns the final `Int -> RunResult` value where `M_E<Step>` is required. This is tracked in ISS-196 and blocks the full example gate.

## Close Notes

# ISS-354: Preserve escaping VM CFG block parameters during simplification

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: area/vmcfg, simplification, control-flow, use-def, compiler-invariant, tests, agent
- Assignee: codex
- Created: 2026-08-11
- Updated: 2026-08-11
- External ref: examples/valid/67_nested_tuple_patterns.lane

## Description
VM CFG simplification decides whether to remove a block parameter from only the
uses in that block. A parameter may instead define an SSA value that remains
live in a dominated successor. Removing the parameter and its incoming edge
argument leaves the later use without a definition.

The former post-bytecode slot allocator accidentally hid this defect when it
coalesced the undefined logical value with an initialized physical slot. Moving
physical-slot planning to its documented VM CFG boundary exposed the invalid
program as a bytecode verifier failure in example 67.

## Design
Use one whole-CFG value-use count when deciding whether a block parameter is
dead. Thread an empty jump block only when each parameter has no use outside
that block, because only local outgoing-edge occurrences can be substituted by
the threading operation. Keep physical-slot planning strict: every active VM
CFG value must have a definition; allocation must never repair an invalid CFG
through incidental coalescing.

## Acceptance Criteria
- [x] A block parameter used only in a dominated successor remains defined after simplification.
- [x] Empty-jump threading does not bypass an escaping block-parameter definition.
- [x] Physical-slot planning reports a structured compiler defect for an active value without a definition.
- [x] The real nested-tuple-pattern example compiles and runs through the integration gate.
- [x] Native tests and formatting/checking gates pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-223
- Discovered from: ISS-337

## Notes
- 2026-08-11: Discovered after the VM CFG-owned physical-slot plan stopped relying on accidental post-bytecode slot coalescing.

## Close Notes

Whole-CFG use counts now own block-parameter retention. Empty-jump threading
requires all parameter uses to be local to the threaded block, and the slot
plan rejects active values without definitions. A focused simplifier regression
preserves a parameter used only by a dominated return; example 67 and the full
integration corpus verify the original program shape.

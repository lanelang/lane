module Examples.Valid09

import Basic.Io.{ println }
import Basic.Ops.*
import Basic.Data.I64.*
import Basic.Trait.Num.{ Add }

pub fn weird_add(a : I64, _b : I64) -> I64 {
  a
}

pub fn use_weird_add(a : I64, b : I64) -> I64 {
  let weird_impl_add : Add[I64] = Add::{ add: weird_add }
  op_add(a, b, op=weird_impl_add)
}

pub fn answer() -> Unit ! Io {
  if use_weird_add(40, 2) == 40 {
    println("weird_add=40")
  } else {
    println("bad:weird_add=40")
  }
}

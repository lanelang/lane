module Examples.Invalid.RefutablePatternLetNested

enum Option[A] {
  none()
  some(A)
}

struct Box {
  value : Option[I64]
}

fn bad(box : Box) -> I64 {
  {
    let Box::{ value: some(x) } = box
    x
  }
}

let value : I64 = bad(Box::{ value: Option::none() })

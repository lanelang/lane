module Examples.Valid10

import Basic.Ops.*
import Basic.Data.I64.*
import Basic.Io.{ println }

pub struct UserId {
  value : I64
}

pub struct OrderId {
  value : I64
}

pub fn user_value(id : UserId) -> I64 {
  id.value
}

pub let user_id : UserId = UserId::{ value: 1 }

pub let order_id : OrderId = OrderId::{ value: 1 }

pub fn answer() -> Unit ! Io {
  if user_value(user_id) == 1 {
    println("user_id=1")
  } else {
    println("bad:user_id=1")
  }
}

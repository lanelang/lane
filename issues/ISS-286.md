# ISS-286: Rewrite every reference when effect specialization copies a type

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: area/effect-specialization, correctness, agent
- Assignee: unassigned
- Created: 2026-07-31
- Updated: 2026-07-31
- External ref: modules/lanec/executable/effect_specialization.mbt, modules/lanec/executable/effect_specialization_data.mbt

## Description
Effect specialization replaces an effect-generic data type with a monomorphic
copy but leaves some references pointing at the original, so the specialized
program does not typecheck.

`examples/valid/37_coroutine_scheduler.lane` reports four `TypeMismatch`
diagnostics on the effect-lowering input, three of the same shape and one
wrapping it:

```
expected = Apply(Constructor(Scheduler), [EffectArgument(Coroutine)])
actual   = Constructor(Scheduler%effect)
```

`Scheduler` is the source type with one effect parameter; `Scheduler%effect` is
the specialized copy with none. The expression was rewritten to the copy while
the type it is checked against still names the generic applied to its effect
argument.

The program runs correctly, because the two types have the same layout. Only the
type level disagrees.

This is also the sole remaining source of `UnknownTypeParameter` after ISS-285:
19 of them appear after monadic-lift for this one program, downstream of the
same mismatch.

## Design
Specialization must rewrite type references everywhere a specialized type can be
named, not only where the pass currently walks. Find the sites it misses by
verifying the specialized program and following the reported types back, rather
than by enumerating suspected cases.

Whether the fix belongs in the walk or in how a specialized copy is registered
should follow from what the missed sites have in common.

## Acceptance Criteria
- [x] `examples/valid/37_coroutine_scheduler.lane` verifies after effect
      specialization.
- [ ] The `UnknownTypeParameter` diagnostics it produces after monadic-lift are
      gone.
- [x] A regression test covers specializing a data type that appears in more
      than one reference position.
- [x] `moon check`, `moon test --target native`, and
      `tools/check-lane-run-examples.sh` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-284
- Discovered from: ISS-284

## Notes
- 2026-07-31: Invisible until now because nothing verifies the linked program.

## Close Notes
The pass rewrote the expression of every reachable binding and none of the
declarations around them. Two places needed it:

- A binding the pass keeps in place. It carries no effect parameters of its own,
  so it is never copied, but its type can still name a data type that was.
- A data constructor or operation whose owner carries no effect parameters, for
  the same reason. `RunResult` in the example holds a `Scheduler[Log]`, and
  `Scheduler` is specialized while `RunResult` is not.

Declarations run after every instance has been requested, so each target already
exists. Owners that do carry effect parameters are skipped: their declarations
mention those parameters, and a copy exists per instance already.

With this and ISS-285, **every** `examples/valid` program now verifies on the
effect-lowering input — from 27 failing programs and 405 diagnostics to none.

The `UnknownTypeParameter` diagnostics after monadic-lift are not resolved here.
They no longer originate at the input, so they are introduced by effect lowering
and belong with the remaining work in ISS-284.

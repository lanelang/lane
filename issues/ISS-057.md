# ISS-057: Consolidate typecheck application inference

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lanec, area/typecheck, architecture, agent
- Assignee: unassigned
- Created: 2026-07-02
- Updated: 2026-07-02
- External ref: none

## Description
The typechecker has no deep seam for application checking. Ordinary calls,
contextual calls, checked-expression calls, function literals, effect operation
calls, and nominal constructor calls each carry pieces of the same policy:
arity validation, named or auto argument handling, generic type argument
inference, expected-result collection, checked argument construction, effect
collection, and `CannotInferExpression` diagnostics.

This makes future source features risky because a new callable form can update
one path while leaving another path with different inference or diagnostic
behavior. The recent structured inference diagnostic work also showed that
adding one semantic cause requires touching many call and function-literal
sites instead of one application layer.

## Design
Introduce a deeper application-checking interface in the typechecker. The
interface should accept a callable shape, source arguments, an optional expected
result, and the desired checking mode, then return an instantiated callable
signature plus checked arguments/effects or a single structured diagnostic path.

Adapters for ordinary function calls, contextual calls, operation calls, and
constructors should describe their callable-specific inputs without reimplementing
the shared inference and arity policy.

Non-goals:
- Do not redesign the whole typechecker in this issue.
- Do not change user-visible diagnostics unless the new shared path exposes
  intentional improvements that are covered by snapshots.

## Acceptance Criteria
- [x] Ordinary calls, contextual calls, checked-expression calls, effect operation calls, and constructor calls share one application/inference policy seam.
- [x] Type argument inference and expected-result collection are not duplicated across call-like forms.
- [x] `CannotInferExpression` reason selection for call-like forms is centralized.
- [x] Regression tests cover at least one generic function call, contextual call, effect operation call, and generic constructor call through the shared path.
- [x] `moon test modules/lanec/typecheck --warn-list +73` passes.
- [x] `moon test modules/lanec/compile --warn-list +73` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-055
- Discovered from: none

## Notes
- 2026-07-02: Created from compiler architecture review. This is currently the highest-risk compiler design issue because application inference is a shallow interface spread across multiple typecheck modules.
- 2026-07-02: Started implementation. First pass will preserve behavior while centralizing shared application inference policy.

## Close Notes
- 2026-07-02: Added application-level helpers for inference diagnostics, arity checks, and type argument inference in `type_arguments.mbt`. Migrated ordinary calls, contextual calls, checked-expression calls, effect operation calls, function literal arity checks, and enum constructor type argument inference to the shared seam. Preserved constructor diagnostic precedence by allowing variant inference to collect expected-result constraints before payload constraints. Verified with `moon test modules/lanec/typecheck --warn-list +73`, `moon test modules/lanec/compile --warn-list +73`, and `moon info && moon fmt`.

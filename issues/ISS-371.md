# ISS-371: Reduce structural amplification in Wasm emission

## Metadata
- Type: task
- Status: open
- Priority: 1
- Labels: area/loisvm, area/wasm, backend, optimization, code-size, architecture, tests, agent
- Assignee: unassigned
- Created: 2026-08-15
- Updated: 2026-08-15
- External ref: examples/valid/37_coroutine_scheduler.lane

## Description
Wasm emission remains the largest structural size amplifier for the Coroutine
Scheduler. The current bytecode has 61 functions and 350 instructions, while
the emitted module has 115 defined functions, 10,551 rendered instructions,
764 locals, 516 structured `if` nodes, 180 `unreachable` nodes, and 87 table
entries. The instruction ratio is approximately 30.15 to one.

The largest generated bodies expand 20 to 27 bytecode operations into 476 to
518 Wasm instructions, and one callable adapter expands to 301 instructions.
Different IRs count instructions at different granularity, so the ratio is not
itself a correctness defect, but it identifies repeated control-flow, runtime
guard, ARC, and indirect-call machinery as the dominant code-size owner.

## Design
Give the Wasm emission owner structured expansion accounting by bytecode
function, opcode family, runtime guard, ARC action, control-flow scaffold,
helper, and entry/runtime lifecycle. Use it to identify repeated emitted
structures and remove duplication at the deepest owner that possesses the
required proof.

Verifier-established bytecode invariants may justify deleting redundant
runtime guards; repeated implementation sequences may move behind canonical
helpers when that reduces total code without changing ownership or trap
behavior. Keep dynamic checks whose facts are not statically proven. Do not
change source semantics, weaken verifier contracts, add per-example branches,
or use arbitrary function-size and call-count thresholds.

## Acceptance Criteria
- [ ] Explore attributes emitted Wasm instructions and locals to structured
      expansion categories without parsing WAT text.
- [ ] The dominant repeated runtime guard, control-flow, ARC, and callable
      adapter sequences are identified with their proof owner.
- [ ] At least one dominant repeated structure is eliminated or shared at its
      canonical owner, with no duplicated policy in the emitter.
- [ ] On a fixed bytecode input, rendered Wasm instructions and at least two of
      locals, structured `if`, `unreachable`, or table entries decrease
      strictly from the recorded baseline.
- [ ] Emitted modules validate and preserve interpreter/JIT behavior, traps,
      ARC ownership, callable ABI checks, and source evaluation semantics.
- [ ] Close notes record both bytecode-normalized and end-to-end Explore
      comparisons so upstream reductions are not credited to Wasm emission.
- [ ] `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, and
      `moon test --target native` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-280, ISS-337, ISS-344, ISS-354, ISS-360, ISS-368, ISS-369
- Discovered from: ISS-356

## Notes
- 2026-08-15: Earlier optimization reduced the end-to-end module from 13,060
  to 10,551 rendered Wasm instructions, but the bytecode-to-Wasm amplification
  ratio changed only from approximately 30.66 to 30.15.

## Close Notes


# ISS-099: Make formatter diagnostics use CompilerDiagnostic

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/diagnostics, area/formatter, refactor, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Remove the separate formatter diagnostic model and make formatter parse/lex errors produce the shared `CompilerDiagnostic` type from `modules/lanec/diagnostic`.

## Design
Update `modules/lanec/format` APIs so `format_source` and `format_source_with_options` return `Result[String, Array[@lanec_diagnostic.CompilerDiagnostic]]`. Delete `FormatDiagnostic`, `FormatDiagnosticLabel`, `FormatDiagnosticLabelStyle`, and `Pretty for FormatDiagnostic`. Construct formatter diagnostics through the shared compiler diagnostic model. Do not make `lanec/format` depend on `lanec/compile`.

## Acceptance Criteria
- [x] `lanec/format` depends on `modules/lanec/diagnostic`, not `modules/lanec/compile`.
- [x] Formatter APIs return `Array[CompilerDiagnostic]` on failure.
- [x] All `FormatDiagnostic*` types are removed.
- [x] Formatter tests assert shared diagnostic structure or source-aware rendering.
- [x] `lane fmt` callers compile against the new formatter diagnostic type.
- [x] `moon info && moon fmt`, `moon check --warn-list +73`, and formatter/lane tests pass.

## Relationships
- Depends on: ISS-098
- Parent: ISS-097
- Related: ISS-096
- Discovered from: ISS-096

## Notes
- 2026-07-04: Formatter diagnostics are source diagnostics; they should not be command reports or compact pretty output.

## Close Notes
Updated `lanec/format` to return `Array[@diagnostic.CompilerDiagnostic]` from `format_source` and `format_source_with_options`, deleted the formatter-specific diagnostic structs/enums/pretty implementation, and updated `lane fmt` command errors to carry the shared compiler diagnostic type. The formatter parse diagnostic test now snapshots the whole shared diagnostic structure. Verification: `moon info`; `moon fmt`; `moon check --warn-list +73`; `moon test modules/lanec/format --warn-list +73`; `moon test modules/lane --warn-list +73`; `moon test modules/lane/lsp --warn-list +73`; `git diff --check`.

# ISS-039: Add Lane effect syntax and symbol namespaces

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lanec, area/effects, area/syntax, area/resolve, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: ../spec/language-spec.typ

## Description
Lane source needs first-class effect declarations and effect operation
identities before Buslane lowering can consume them. Today the syntax,
resolver, symbol registry, and module interface layers only model ordinary
types, values, fields, and variants.

## Design
Add source AST forms for `effect` declarations and operation declarations,
extend kind syntax with `Effect`, and introduce distinct effect constructor and
operation symbol namespaces. Resolution should keep operations out of the
ordinary value namespace and preserve source spans/presentation in `lanec`
structures only.

## Acceptance Criteria
- [x] Syntax AST and parser represent effect declarations and operation
  signatures from the spec.
- [x] Symbol registry has distinct effect constructor and operation identities.
- [x] Resolver resolves effect constructors and operation names without using
  ordinary value lookup.
- [x] Module interfaces can export/import effect declarations and operation
  signatures.
- [x] Source spans and display names remain in `lanec`, not Buslane.
- [x] Parser/resolver/module-interface tests cover effect declarations.
- [x] `moon test modules/lanec` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: ISS-035
- Parent: ISS-037
- Related: none
- Discovered from: ISS-037

## Notes
- 2026-07-01: Split because Buslane lowering cannot honestly lower source
  effects until Lane owns the source namespaces.

## Close Notes
- 2026-07-01: Added Lane effect declarations and operation syntax through
  parser, symbol registry, resolver, checked source, ANF pass-through, module
  interface artifact text, import freshening, and interface fingerprinting.
  Added focused parser/resolver/typecheck/compile tests. Verified with
  `moon test modules/lanec/parser modules/lanec/resolve modules/lanec/typecheck modules/lanec/compile --warn-list +73`,
  `moon test --warn-list +73`, `moon test modules/lanec --warn-list +73`
  (no aggregate test entry), `moon info && moon fmt`, and `git diff --check`.

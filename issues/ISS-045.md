# ISS-045: Check Lane handler result and resume types

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lanec, area/typecheck, area/effects, tests, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: ../spec/language-spec.typ

## Description
Handler operation arms and the final branch must agree on the handler result
type. Resume binders must receive function types whose parameter is the
operation result type and whose latent effect is the handler residual effect.

## Design
Extend handler checking with the result-type and residual-effect judgment. Check
the handled expression effect, remove handled concrete effects from the
residual set, type final and operation-arm bodies against a common result type,
and bind resume continuations as deep-handler resume functions.

## Acceptance Criteria
- [x] Final branch body and operation-arm bodies are checked against a common
  handler result type.
- [x] Resume binders receive `(Q) -> R ! E_res` types, where `Q` is the
  operation result type, `R` is the handler result type, and `E_res` is the
  residual handler effect.
- [x] Handled effects are removed from the handled expression effect when
  computing the handler expression effect.
- [x] Operation arm direct effects contribute to the residual effect according
  to the current Lane effect judgment.
- [x] Tests cover resume typing, final/body result mismatches, and unhandled
  residual effects.
- [x] `moon check modules/lanec --warn-list +73` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: ISS-044
- Parent: ISS-041
- Related: none
- Discovered from: ISS-041

## Notes
- 2026-07-01: This issue completes semantic handler typing before lowering
  source payload patterns.

## Close Notes
- 2026-07-01: Added handler result typing, final/operation body checking,
  resume function binding, and residual effect computation that removes handled
  effects while preserving final/direct arm effects. Validation: `moon test
  modules/lanec/typecheck --target native --warn-list +73`, `moon check
  modules/lanec --warn-list +73`, `moon test --target native --warn-list +73`,
  `moon info && moon fmt`, `git diff --check`.

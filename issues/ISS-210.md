# ISS-210: Implement opaque host objects in LoisVM runtime

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: area/loisvm, area/runtime, ownership, tests
- Assignee: Codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description
Add the execution-local Host Object Table, generational handles, owned host results, finalizers, resource limits, and erased host-call value model required by `Opaque`.

## Design
Store payload and finalizer in table entries and place only a backend-private handle in the Lane ARC wrapper. Parameters resolve to call-scoped borrowed payloads; results transfer one independently owned payload-finalizer pair through atomic adoption. Finalizers receive no Runtime Context, are non-observable and non-retriable, and may panic fatally.

## Acceptance Criteria
- [x] `ExecutionConfig.max_host_objects` has consistent disabled, bounded, and invalid-value semantics.
- [x] Handles validate execution identity, slot index, and generation.
- [x] Final release removes the entry and invokes its finalizer exactly once.
- [x] Partial result adoption finalizes the pending payload without leaking a slot.
- [x] Host SDK exposes erased core values and typed registration adapters without payload brands.
- [x] Focused lifecycle, aliasing, limit, stale-handle, and panic tests pass.

## Relationships
- Depends on: ISS-209
- Parent: ISS-205
- Related: ISS-187
- Discovered from: ISS-205

## Notes
- 2026-07-16: Started the execution-local generational table, erased payload, typed projection, limit, and finalization implementation.

## Close Notes
Added an execution-local generational Host Object Table behind
`ExecutionRuntime`, while host bindings receive only an abstract
`RuntimeContext`. Added borrowed and owned erased Host Object values,
transactional adoption, exact-once finalization, live-object limits, and typed
parameter/result adapters using the accepted unbranded trusted projection.

Verified with `moon check`, 14 focused runtime tests, interpreter and Wasm
package tests, the full 819-test repository suite, and `git diff --check`.
Native test compilation emits two expected incompatible-pointer warnings at the
deliberate `%identity` projection required by the accepted unchecked-cast
boundary; no MoonBit diagnostics remain.

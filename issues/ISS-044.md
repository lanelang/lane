# ISS-044: Check handler operation tables and payload patterns

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lanec, area/typecheck, area/patterns, area/effects, tests, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: ../spec/language-spec.typ

## Description
Each handler `with` block must correspond to exactly one concrete effect, and
each handled operation must have useful and exhaustive payload-pattern arms.
The type checker must reject mixed-effect blocks, duplicate handled effects,
unknown operations, missing operation coverage, and non-useful payload arms.

## Design
Add checked handler operation-table construction. Group arms by concrete effect
owner, validate that each block handles one effect, reject duplicate handled
effects across blocks, and reuse the existing pattern matrix machinery to check
payload patterns per operation.

## Acceptance Criteria
- [x] Checked handler data groups operation arms by concrete handled effect.
- [x] A `with` block containing operations from multiple effects is rejected.
- [x] A handler containing two `with` blocks for the same concrete effect is
  rejected.
- [x] Every operation declared by a handled effect is covered by exhaustive
  payload patterns.
- [x] Redundant or non-useful handler operation arms are rejected or diagnosed
  consistently with existing pattern usefulness diagnostics.
- [x] Tests cover direct arms, payload-pattern arms, missing operation
  coverage, duplicate handled effects, and mixed-effect blocks.
- [x] `moon check modules/lanec --warn-list +73` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: ISS-043
- Parent: ISS-041
- Related: none
- Discovered from: ISS-041

## Notes
- 2026-07-01: This issue owns operation grouping and payload-pattern validity,
  but not resume/final result typing.

## Close Notes
- 2026-07-01: Added checked handler tables with per-effect grouping,
  duplicate/mixed-effect diagnostics, and payload pattern usefulness/exhaustive
  coverage checks. Validation: `moon test modules/lanec/typecheck --target
  native --warn-list +73`, `moon check modules/lanec --warn-list +73`, `moon
  info && moon fmt`, `git diff --check`.

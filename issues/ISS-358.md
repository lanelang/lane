# ISS-358: Give ANF and function plans explicit provenance

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/anf, area/loisvm-lowering, provenance, identity, tests, agent
- Assignee: codex
- Created: 2026-08-11
- Updated: 2026-08-11
- External ref: modules/lanec/loisvm_lowering/model.mbt

## Description
ANF functions have no identity and LoisVM lowering allocates FunctionId values
through several direct `plans.push` paths. A later observer therefore cannot
distinguish preserved functions from closures, adapters, initializers, and
other generated functions without reconstructing lowering behavior.

## Design
ANF lowering assigns deterministic report-local function identities. Replace
the bare planned-entry array with one atomic function-plan abstraction carrying
the entry and its typed origin. Every FunctionId allocation goes through one
owner. Generated functions use explicit roles rather than fabricated source
parents.

## Acceptance Criteria
- [x] Every ANF function has a deterministic identity preserved by simplification.
- [x] Bound functions retain their final Buslane value anchor when one exists.
- [x] Every planned FunctionId is created together with exactly one typed origin.
- [x] Nested, recursive, initializer, adapter, intrinsic, layout, and import
      planning paths cannot bypass the provenance owner.
- [x] Focused tests cover source and synthetic origins.

## Relationships
- Depends on: ISS-357
- Parent: ISS-356
- Related: ISS-354
- Discovered from: ISS-356

## Notes
- 2026-08-11: Claimed after ISS-357 established the shared Explore metric and
  function-scale vocabulary.
- 2026-08-11: ANF now assigns deterministic preorder identities at function
  construction and carries the binding's final Buslane value through
  simplification. A black-box regression covers top-level and local anchors.
- 2026-08-11: Replaced the writable planned-entry array with FunctionPlanTable.
  Its single-item and recursive-group operations allocate FunctionId, entry,
  and typed origin together. All source and synthetic planning paths consume
  that owner, and a focused white-box invariant test covers source and
  initializer origins.

## Close Notes
- Deterministic ANF identities survive simplification and retain final Buslane
  value anchors where a binding exists.
- FunctionPlanTable is the only FunctionId allocation boundary in LoisVM
  planning; direct array insertion is no longer representable.
- Verified with `moon test --target native` (1631 tests).

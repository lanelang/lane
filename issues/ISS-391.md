# ISS-391: Preserve source-written and CPS-introduced function provenance

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: area/lanec, area/effect-lowering, area/anf, architecture, observability, tests, agent
- Assignee: codex
- Created: 2026-08-27
- Updated: 2026-08-27
- External ref: issues/ISS-369.md

## Description
Runtime ANF currently records an optional source value for each function. A
missing source means only that no top-level source binding owns the function;
it does not distinguish a source-written nested function from a function
introduced by handler elaboration, monadic lowering or selective CPS.

ISS-369 therefore reports the factual `Anonymous` origin instead of guessing a
CPS origin from missing metadata. Explore cannot yet separate source-semantic
callable escape from lowering-introduced callable flow.

## Design
The transformation that introduces a function owns its origin. Define a small,
closed function-origin vocabulary at the effect-lowering/Runtime ANF boundary
and preserve it through ANF simplification and physical function planning.
Source elaboration marks source-written top-level and nested functions;
effect-lowering stages mark the functions they introduce. Later stages only
copy the origin and may add structural roles such as Callable Adapter or
Representation Worker.

Do not infer origin from a missing source binding, generated names, spans,
function order or rendered IR. Origin is diagnostic and observational metadata;
it must not affect optimization legality or runtime semantics.

## Acceptance Criteria
- [x] Every Runtime ANF function has an explicit source-written or
      lowering-introduced origin in addition to any source binding identity.
- [x] Handler, monadic and selective-CPS introduced functions retain distinct
      typed origins through ANF simplification and Physical Program emission.
- [x] Explore distinguishes source-written anonymous flow from CPS-introduced
      flow without using names, text or absence of source metadata.
- [x] Existing callable-adapter, representation-worker, initializer and runtime
      support roles remain structural facts produced by their owning stages.
- [x] Black-box Explore coverage includes one source nested function and one
      CPS-introduced continuation with stable, distinct origins.
- [x] `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, and
      `moon test --target native` pass.

## Relationships
- Depends on: none
- Parent: ISS-373
- Related: ISS-368, ISS-369
- Discovered from: ISS-369

## Notes
- 2026-08-27: Split from ISS-369 because final physical callable-flow analysis
  cannot reconstruct which earlier transformation introduced an anonymous
  function. Adding that guess there would create a second provenance producer.
- 2026-08-27: Implementation started. Provenance will remain transient compiler
  metadata rather than changing persisted Buslane syntax: effect lowering owns
  origin creation, Core optimization explicitly transfers it across rewrites,
  and Runtime ANF requires and internalizes it.

## Close Notes
- 2026-08-27: Added a transient, producer-owned `FunctionOriginCatalog`. Source
  ingestion, handler elaboration, monadic transformation and selective CPS now
  assign typed origins; rebuilding optimizations must explicitly transfer them,
  and Runtime ANF rejects missing provenance instead of inferring it.
- 2026-08-27: Runtime ANF, Physical Program and Explore now preserve semantic
  origin independently from structural roles such as representation worker and
  callable adapter. Explore protocol 10 exposes the expanded closed vocabulary.
- 2026-08-27: Added black-box Explore coverage for a source-written nested
  function and lowering-introduced functions, plus focused catalog and Core
  optimization boundary tests.
- 2026-08-27: Verified `moon info`, `moon fmt`, strict native check, all 1,721
  native tests, and all 7 `lane_wasm` Wasm tests.

# ISS-408: Certify the Wasm-only release baseline

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/lane, area/basic, area/wasm, ci, release, tests, agent
- Assignee: codex
- Created: 2026-08-28
- Updated: 2026-08-28
- External ref: modules/lanec/docs/wasip1-webassembly-target-rfc.md

## Description
The WebAssembly-only migration and Wasmoon 0.12.6 upgrade passed local checks,
but the release contract needs one durable, current baseline. Historical test
and optimization evidence predates the removal of the semantic runtime.

## Design
Use the existing public compiler and command boundaries. Pin and record the
Basic revision and Wasmoon dependency family, run native compiler checks, the
complete examples corpus, Basic under both Wasmoon execution modes, and the
Lane WebAssembly Profile feature probes. Inspect emitted raw Wasm through its
public module contract to prove that legacy Lane runtime imports and container
metadata are absent.

Prefer strengthening the existing quality gate or public black-box tests over
adding a second integration script. Build `lane.exe` once per gate.

## Acceptance Criteria
- [x] The exact Basic revision and Wasmoon dependency family are recorded.
- [x] Native check, formatting, tests, release build, full examples and Basic
      suite pass at the pinned revisions.
- [x] Wasmoon interpreter and JIT execute the same profile probes and Basic
      behavior.
- [x] A public contract test verifies `_start`, required `memory`/WASI imports,
      and absence of the deleted `lane.runtime.v1`, `lane.entry`, and linked
      executable container.
- [x] CI invokes the durable release gate without rebuilding Lane redundantly.

## Relationships
- Depends on: none
- Parent: ISS-407
- Related: ISS-353, ISS-362, ISS-400, ISS-405
- Discovered from: ISS-407

## Notes
- 2026-08-28: Initial post-migration release qualification task.

## Close Notes

Closed on 2026-08-28 against pinned Basic `807f78b4bd31` and Wasmoon,
Wasmoon JIT, wasm_core and Milkir `0.12.6`.

The public backend-conformance package now executes probes for Bulk Memory,
Reference Types, Typed Function References, Tail Call, Exception Handling,
Multi-value, Fixed-width SIMD and Extended Constant Expressions under both
Wasmoon modes. A complete raw-module observation fixes the Wasm magic, canonical
WASI import, runtime-control globals, `_start`, and `memory` export contract.
The quality gate reuses its one release `lane.exe` build for Basic JIT,
Basic `--no-jit`, and examples.

Verification passed with `moon info`, `moon fmt`,
`moon check --target native --warn-list +73`, 1709 native tests, the release
Lane build, both 12-test Basic runs, the complete examples gate, and
`git diff --check`.

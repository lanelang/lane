// Valid: one alias parameter header may contain multiple binders.

module Examples.Valid17

import Basic.Ops.*
import Basic.Data.I64.*
import Basic.Io.{ println }

pub type Left[A, _B] = A

pub let left_value : Left[I64, Bool] = 42

pub fn answer() -> Unit ! Io {
  if left_value == 42 {
    println("42")
  } else {
    println("bad:42")
  }
}

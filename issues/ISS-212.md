# ISS-212: Execute opaque runtime imports through Wasm

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/loisvm, area/wasm, area/wasmoon, integration, tests
- Assignee: Codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description
Map `Opaque` runtime imports through generated Wasm adapters while keeping host objects outside linear memory and sharing one Host Object Table with the execution instance.

## Design
Treat `Opaque` as an abstract compiler/runtime ABI kind. Backend-private Wasm transport carries generational handles, runtime adapters resolve them through Runtime Context, and host bindings see only borrowed payloads or owned results. Do not serialize or expose the physical handle encoding.

## Acceptance Criteria
- [x] Wasm compilation accepts Runtime Import signatures containing `Opaque`.
- [x] Generated adapters transport handles without storing host references in linear memory.
- [x] Wasmoon execution uses the execution instance's shared Host Object Table.
- [x] Interpreter and Wasm agree on observable calls, limits, panic behavior, and normal finalization.
- [x] Focused Wasm compiler and native Wasmoon tests pass.

## Relationships
- Depends on: ISS-210
- Parent: ISS-205
- Related: ISS-204
- Discovered from: ISS-205

## Notes
- 2026-07-16: Implementation started after ISS-211 established the interpreter behavior and shared runtime transaction API.

## Close Notes
- 2026-07-16: Added a backend-private Wasm sidecar that maps one packed `i64` generational token to each shared-runtime Host Object handle. Linear memory stores only a 16-byte ARC wrapper containing that token; host-language payloads remain outside Wasm memory.
- 2026-07-16: Generated Opaque adapters load tokens for borrowed parameters, release transferred wrappers after calls, allocate owned result wrappers through a private exported service, and destroy final wrappers through a private host import.
- 2026-07-16: Unified out-of-band Wasmoon failures under `ExecutionError`, preserving Host Object limits, runtime-import failures, internal token failures, heap limits, and normal global finalization. Split host adapter logic from `load.mbt` into cohesive runtime-import and Host Object files.
- 2026-07-16: Added compiler contract coverage plus native Wasmoon tests for shared aliases, normal global shutdown, object/heap adoption rollback, and explicit-close failure. Verified `moon test modules/loisvm/wasm` (43 passed), `moon info && moon fmt`, full `moon check`, full `moon test` (5 wasm + 632 wasm-gc + 191 native passed), and `git diff --check`. Native compilation retains the accepted unchecked host-projection pointer warnings from ISS-210.

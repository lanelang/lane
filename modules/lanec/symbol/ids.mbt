///|
pub struct TypeSymbolId {
  index : Int
} derive(Debug, Eq, Compare)

///|
pub struct TypeParameterId {
  index : Int
} derive(Debug, Eq, Compare)

///|
pub struct TypeMemberSymbolId {
  index : Int
} derive(Debug, Eq, Compare)

///|
pub struct ValueSymbolId {
  index : Int
} derive(Debug, Eq, Compare)

///|
pub struct FieldSymbolId {
  index : Int
} derive(Debug, Eq, Compare)

///|
pub struct VariantSymbolId {
  index : Int
} derive(Debug, Eq, Compare)

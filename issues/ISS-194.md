# ISS-194: Rewire compilation and command execution

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: area/lanec, area/lane, integration, artifacts, tests
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: modules/lane/run.mbt

## Description
Route source execution, linking, linked artifacts, and runtime loading through Whole-Program Elaboration and schema-v2 LoisVM images.

## Design
Keep `.lmo` as Buslane module objects and `.lbp` as final LoisVM bytecode. Move target convenience APIs to orchestration, update inspect output and artifact tests, and ensure `lane run` and `lane link` share the same executable path.

## Acceptance Criteria
- [x] `lane run` and `lane link` invoke Whole-Program Elaboration exactly once.
- [x] `.lbp` roundtrips schema-v2 bytecode and `lane inspect` reports initializer and globals.
- [x] No command or module-link package bypasses Executable Program.
- [x] Command, artifact, and runtime integration tests pass.

## Relationships
- Depends on: ISS-192, ISS-193
- Parent: ISS-186
- Related: ISS-132
- Discovered from: ISS-186

## Notes

- 2026-07-14: Claimed after `6747d7c` made schema-v2 images executable on both LoisVM backends.
- 2026-07-14: Consolidated command orchestration behind shared linked-program elaboration and executable-image lowering helpers; production `lane` no longer imports LoisVM lowering directly.
- 2026-07-14: Added schema-v2 `.lbp` roundtrip/inspect coverage and top-level global integration through `lane run`, `lane link`, and `lane exec`.

## Close Notes

Closed with `.lmo` remaining target-independent Buslane module objects and `.lbp` carrying final schema-v2 LoisVM bytecode. Lane command tests pass 49/49 and artifact-binary tests pass 5/5.

# ISS-374: Unify module parsing, reachability, and dependency diagnostics

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/frontend, area/module, diagnostics, architecture, refactor, tests, agent
- Assignee: unassigned
- Created: 2026-08-16
- Updated: 2026-08-17
- External ref: modules/lanec/module/frontend/input.mbt

## Description
Module Frontend stores successful modules, recovered headers, source syntax,
and diagnostics in parallel arrays. Import-reachable compilation then computes
source reachability, recovered-header reachability, graph validation, and graph
construction through separate traversals; the compile facade rebuilds the graph
again. The semantic workspace consumes the same parallel arrays by positional
convention.

Module dependency failures are also classified and rendered twice. Module
Frontend and Module Compile independently own missing-import and import-cycle
kinds, E4000/E4002/E4003 codes, labels, notes, helps, and candidate facts.

## Design
Represent each parsed source as one record containing its source identity,
text, concrete syntax, identifier index, diagnostics, and either a valid module
or a recovered header. Build strict and tolerant dependency results from that
single source collection.

Make Module Graph the sole producer of import reachability, missing dependency
facts, cycle facts, and topological order. Strict compilation should consume a
completed graph directly instead of receiving a `ModuleInputSet` and rebuilding
it. Tolerant workspace analysis consumes the same parsed-source and graph facts
with an explicit unavailable-source policy.

Define one structured Module Dependency Failure and one diagnostic adapter in
Module Frontend. Source graphs and interface-artifact closure analysis provide
only provenance and candidate inputs; Module Compile must not reclassify or
rerender the same dependency failure.

## Acceptance Criteria
- [x] One parsed-source value owns syntax, recovered header, diagnostics, and
      valid/invalid module state without parallel positional arrays.
- [x] Strict import-reachable parsing returns the one validated Module Graph
      consumed by compilation; no caller rebuilds it or recomputes reachability.
- [x] Tolerant semantic-workspace topology uses the same graph implementation
      and explicitly records unavailable parsed modules.
- [x] Missing import, import cycle, and duplicate-input dependency failures have
      one structured classification and diagnostic adapter. A missing root is
      unrepresentable at the validated graph seam; losing one afterward is a
      structured compiler defect.
- [x] Source and interface-artifact dependency failures preserve their distinct
      provenance while sharing codes, messages, facts, and suggestions.
- [x] Black-box compile and Semantic Workspace tests cover reachable invalid
      sources, unrelated invalid sources, missing imports, cycles, and duplicate
      modules without testing parallel-array layout.
- [x] The old reachability traversals and duplicate dependency diagnostic
      variants are deleted.
- [x] `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, and
      `moon test --target native` pass.

## Relationships
- Depends on: none
- Parent: ISS-373
- Related: ISS-028, ISS-065, ISS-275
- Discovered from: ISS-373

## Notes
- 2026-08-16: The recovered header remains necessary for deciding whether a
  syntactically invalid source is import-reachable; the duplicate parallel
  collections and repeated graph traversals do not.
- 2026-08-17: Implementation started. The refactor will replace the existing
  public parsing and graph-building layers instead of adding a third adapter.
- 2026-08-17: The missing-root case was removed from source dependency
  diagnostics. A strict graph is constructed only from its mandatory parsed
  root; absence after that boundary is an E4599 compiler defect rather than a
  second user-facing dependency condition.

## Close Notes

- 2026-08-17: `ParsedSource` now owns all parse products and the explicit
  available/unavailable state. Strict compilation consumes the validated
  `ModuleGraph` returned by import-reachable parsing, while Semantic Workspace
  feeds the same graph analysis with unavailable parsed sources.
- 2026-08-17: One DFS now owns reachable dependency order, missing imports,
  cycles, and duplicate inputs. `ModuleDependencyFailure` owns E4001/E4002/E4003
  rendering and retains source-input or interface-artifact provenance; the
  former Module Compile diagnostic variants and graph reconstruction paths were
  deleted.
- 2026-08-17: Public graph access returns detached dependency facts so callers
  cannot mutate a validated topology. Black-box compile and workspace tests
  cover reachable and unrelated parse failures plus all dependency failure
  classes; a public graph test covers mutation isolation and a structured test
  covers provenance-independent diagnostic policy.
- 2026-08-17: Verified with `moon info`, `moon fmt`,
  `moon check --target native --warn-list +73`, and all 1,738 native tests.

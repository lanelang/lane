module Examples.Invalid43

enum Pair[A, B] {
  pair(A, B)
}

fn[F : [Type] -> Type] consume_i64(_value : F[I64]) -> I64 {
  0
}

fn bad(value : Pair[I64, Bool]) -> I64 {
  consume_i64(value)
}

let value : I64 = bad(Pair::pair(1, true))

# ISS-242: Split resolver collection by semantic responsibility

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: area/resolve, architecture, refactor, agent
- Assignee: unassigned
- Created: 2026-07-21
- Updated: 2026-07-31
- External ref: modules/lanec/resolve/collect.mbt

## Description
`resolve/collect.mbt` is approximately 3,700 lines and owns declaration precollection, import publication, type and effect resolution, expression and pattern resolution, provenance recording, lookup helpers, and diagnostics. These responsibilities share mutable resolver state but do not form one cohesive implementation unit. Changes to one namespace or syntax family repeatedly touch the same large file and conceal duplicated conventions.

## Design
Mechanically split the existing implementation into same-package files along its natural responsibilities: declaration/import collection, type/effect resolution, expression/operation resolution, pattern resolution, and lookup/provenance support. Keep `Resolver` state and invariants private to the package, preserve dependency direction, and avoid introducing compatibility facades or new packages with one consumer. Resolve concrete duplicated conventions while moving them rather than copying helpers into each file.

## Acceptance Criteria
- [x] Resolver declaration/import, type/effect, expression/operation, pattern, and lookup/provenance implementations have clear same-package ownership.
- [x] `collect.mbt` is removed or reduced to orchestration rather than remaining the implementation sink.
- [x] No helper implementation is duplicated across the resulting files.
- [x] Public resolver interfaces and generated `.mbti` remain unchanged unless a separately justified semantic fix requires otherwise.
- [x] Existing resolver, typecheck, analysis, compile, formatter, and examples behavior remains unchanged.
- [x] `moon info`, `moon fmt`, focused tests, full tests, examples, and `git diff --check` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-237, ISS-238
- Discovered from: none

## Notes
- 2026-07-21: This issue is a locality refactor, not a request to create more public packages or abstractions. File size is supporting evidence; mixed responsibility and repeated semantic conventions are the actual problem.

## Close Notes

- Closed on 2026-07-31 after auditing the already-landed `efc38d63 refactor(resolve): split resolver responsibilities`.
- That commit deleted the 4,155-line `collect.mbt` and mechanically assigned declarations/import publication, types/effects/generic arguments, expressions/operations, patterns, resolver state, and name/provenance lookup to focused same-package files without a compatibility facade.
- The split changed no generated resolver interface. Current focused verification passed: resolve 39/39 and the dependent typecheck, analysis, and compile packages 482/482; the current batch also passed full native tests, examples, formatting, and `git diff --check`.

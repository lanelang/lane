# ISS-188: Extract the module link package

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: area/lanec, refactor, module-link, tests
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: modules/lanec/module/compile

## Description
Move the linked-program model and `link_*.mbt` implementation out of `lanec/module/compile` so executable elaboration can consume linking without depending on LoisVM orchestration.

## Design
Create `lanec/module/link` as a coherent target-independent package. Preserve public behavior and remove the `module/compile` reverse dependency on `lanec/loisvm_lowering`; orchestration may depend on both packages.

## Acceptance Criteria
- [x] `lanec/module/link` owns Linked Program construction, validation, remapping, identity, and diagnostics.
- [x] No package cycle or execution-target dependency exists in the link package.
- [x] Existing compile, link, artifact, and command tests pass unchanged.
- [x] Generated interfaces reflect only intentional ownership moves.

## Relationships
- Depends on: ISS-187
- Parent: ISS-186
- Related: ISS-148
- Discovered from: ISS-186

## Notes
- 2026-07-14: Ready after the architecture documentation milestone.
- 2026-07-14: Claimed after commit `1dfab50` closed ISS-187 and made this the sole ready issue.

## Close Notes
Extracted the target-independent link model and implementation into `lanec/module/link`, leaving `module/compile` with compilation and artifact adapters. LoisVM lowering helpers now live at the artifact-binary orchestration boundary, so `module/compile` no longer imports `lanec/loisvm_lowering`. Updated generated interfaces expose the ownership move without changing linked-program behavior. Verification: `moon info`; `moon fmt`; `moon check modules/lanec/module/link modules/lanec/module/compile modules/lanec/compile modules/lane --target all --warn-list +73`; `moon test modules/lanec/compile --target all` (100/100 each target); `moon test modules/lanec/compile/artifact_binary --target all` (4/4 each target); `moon test modules/lane --target native` (48/48); `git diff --check`.

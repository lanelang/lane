# ISS-196: Make abstract residual contexts safe across CPS answer changes

## Metadata
- Type: bug
- Status: in_progress
- Priority: 0
- Labels: area/lanec, effects, cps, architecture, examples
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-15
- External ref: ADR-0067

## Description
ADR-0067 models an abstract effect parameter as one ordinary companion type/value carrying an opaque context. That context is tied to the Answer type at which its concrete handler dictionary was created. A handler with abstract residual effect `E` changes the handled body's local answer to `M_E<H>` and currently reuses the outer opaque `E` context at that different answer. This is not type- or semantics-preserving.

Example 37 exposes the defect in `run_task[E]`: a handled Coroutine clause must return `M_E<Step>`, but execution returns the outer `Int -> RunResult` answer and later calls it through the residual callable ABI, producing an indirect-call type mismatch in both interpreter diagnostics and Wasm.

## Design
Run whole-program reachable effect specialization after linking and executable selection, before handler elaboration and selective CPS. Clone each reachable callable for its concrete kind-`Effect` arguments, rewrite calls to those instances, and retain ordinary kind-`Type` polymorphism. This makes handler residual rows concrete so `mon-trans` can construct answer-correct relay dictionaries. A reachable handler whose residual row remains abstract is a compile-time error; do not hide the mismatch with a callable adapter or runtime effect lookup.

## Acceptance Criteria
- [x] A grill decision records how abstract contexts cross Answer changes and updates ADR-0067.
- [ ] `examples/valid/37_coroutine_scheduler.lane:answer` prints `1112211322` in both interpreter and Wasm paths.
- [ ] Focused tests cover a handler with an abstract residual effect whose clause suspends before the outer handler completes.
- [ ] Existing effect-lowering, CPS, LoisVM lowering, and example suites remain green.
- [ ] No `perform`, handler stack, runtime effect tag, or effect-specific LoisVM instruction is introduced.

## Relationships
- Depends on: none
- Parent: ISS-186
- Related: ISS-195, ADR-0067
- Discovered from: ISS-195

## Notes
- 2026-07-14: Instrumentation traced final call `50 -> 147`: `run_task` called an erased Task body with local answer `M_E<Step>`, then received an `Int -> RunResult` adapter. VM CFG and finalized bytecode preserved the intended closure/capture slots, ruling out slot allocation and ARC corruption. The mismatch originates in abstract residual-context reuse during `Install` lowering.
- 2026-07-15: The grill selected whole-program reachable effect specialization before selective CPS. ADR-0067 now defines its placement, worklist roots, preservation of ordinary type polymorphism, and compile-time rejection of any answer-changing handler whose residual row remains abstract.
- 2026-07-15: Implemented the executable-boundary specialization worklist for reachable callables and nominal data instances. Effect arguments are substituted before CPS while ordinary type parameters remain generic; cloned local binders receive specialized metadata. Focused executable/effect/CPS/LoisVM suites pass, and coroutine scheduler example 37 now prints `1112211322`.
- 2026-07-15: Added a focused regression that models a handler with abstract residual `E` and verifies its callable, action parameter, and resume metadata all become concrete `Log` before answer-type CPS. The authoritative example script also passes every fixture.

## Close Notes

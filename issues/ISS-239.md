# ISS-239: Consolidate Buslane substitution ownership

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/buslane, area/loisvm-lowering, backend, refactor, agent
- Assignee: unassigned
- Created: 2026-07-21
- Updated: 2026-07-31
- External ref: modules/buslane/buslane.mbt, modules/lanec/loisvm_lowering/calls.mbt

## Description
Buslane owns `TypeSubstitution`, `Type::substitute`, and `Effect::substitute`, but LoisVM lowering maintains a second `TypeArgumentBinding` representation and private recursive type/effect substitution. The implementations differ in lookup order, chained replacement behavior, kind-mismatch handling, and function-effect traversal. Lowering correctness therefore depends on undocumented stage invariants instead of one substitution contract.

## Design
Deepen the Buslane substitution module so it explicitly supports the composition, shadowing, and validation semantics needed by LoisVM lowering. Convert lowering bindings to that representation once, then delete the duplicate recursive substitution implementation. Preserve explicit lowering errors for type/effect kind mismatches rather than silently ignoring invalid replacements. Document whether latent effects are expected to remain at this stage and enforce that invariant.

## Acceptance Criteria
- [x] LoisVM lowering uses the Buslane-owned substitution implementation for types and effects.
- [x] Chained substitutions, binder shadowing, and latest-binding behavior have explicit tests.
- [x] Type-for-effect and effect-for-type mismatches remain structured lowering errors.
- [x] Function parameter, result, and latent-effect traversal has one documented behavior.
- [x] The private `substitute_type` and `substitute_effect` implementations in LoisVM lowering are removed.
- [x] `moon info`, `moon fmt`, focused tests, full tests, examples, and `git diff --check` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-07-21: The current lowering substitution leaves `Function` effects unchanged and searches bindings in reverse order, while the generic Buslane implementation has a different lookup contract.

## Close Notes

- Closed on 2026-07-31 after verifying that the implementation had already migrated LoisVM lowering to `@buslane.TypeSubstitution`.
- `modules/buslane/buslane_wbtest.mbt` covers simultaneous substitution, binder shadowing, overlay precedence, latent-effect traversal, and both argument-kind mismatch directions.
- `modules/lanec/loisvm_lowering/calls.mbt` converts `TypeSubstitutionError` into structured `InvalidTypeApplication` errors and contains no private recursive type/effect substitution.
- Focused native gates passed: `modules/buslane` (45/45) and `modules/lanec/loisvm_lowering` (34/34). Repository-wide gates are re-run as the final validation for this issue batch.

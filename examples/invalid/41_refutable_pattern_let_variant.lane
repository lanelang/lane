module Examples.Invalid.RefutablePatternLetVariant

enum Option[A] {
  none()
  some(A)
}

fn bad(value : Option[Int]) -> Int {
  {
    let some(x) = value
    x
  }
}

let value : Int = bad(Option::none())

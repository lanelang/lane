module Examples.Invalid44

struct Option[A] {
  value : A
}

struct Uses[F : [Type] -> Type] {
  marker : I64
}

offer option_uses : Uses[Option] = Uses::{ marker: 0 }

fn[F : [Type] -> Type] use_context(auto _ctx : Uses[F]) -> I64 {
  0
}

let value : I64 = use_context()

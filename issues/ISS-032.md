# ISS-032: Attach latent effects to Buslane function types

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: area/buslane, area/effects, area/types, agent
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: modules/buslane/docs/adr/0056-function-types-carry-latent-effects.md

## Description
Buslane function types currently only carry parameter and result types. The
effect roadmap requires every function type to carry a latent effect, with pure
functions represented by the empty effect.

## Design
Update the function type shape to include a latent effect. Migrate all pure
function construction sites to use the empty effect, then update equality,
substitution, metadata helpers, pretty output, and tests. Keep the latent
effect as an effect set with optional residual row information, not a flattened
operation set.

## Acceptance Criteria
- [x] Function types store parameter types, result type, and latent effect.
- [x] Existing pure Buslane programs still verify by using the empty effect.
- [x] Type equality and substitution account for latent effects.
- [x] Pretty output includes latent effects where required without noisy output
  for pure functions unless the chosen text format requires it.
- [x] `moon test modules/buslane` passes.
- [x] `moon info && moon fmt` has been run after the change.

## Relationships
- Depends on: ISS-031
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-07-01: Split from the Buslane effect roadmap and ADR-0056.

## Close Notes
- 2026-07-01: Changed Buslane function types to
  `Function(Array[Type], Type, Effect)`, migrated existing pure function types
  to `Empty`, and updated type equality, normalization, and substitution to
  include latent effects. Pretty output omits `! {}` for pure functions and
  prints non-empty latent effects. Canonical text remains backward-compatible
  for pure `fn_type(parameters, result)` and can roundtrip non-empty function
  effects with the three-argument form. Updated Buslane lowering, module object
  remapping, interpreter tests, ANF tests, and generated interfaces. Verified
  with `moon test modules/buslane --warn-list +73`, `moon test
  modules/buslane/text --warn-list +73`, `moon test modules/buslane/interpreter
  --warn-list +73`, `moon check modules/lanec --warn-list +73`, `moon test
  modules/lanec/elaborate --warn-list +73`, `moon test modules/lanec/anf
  --warn-list +73`, `moon test modules/lanec/interpreter --warn-list +73`,
  `moon check modules/lanec/compile --warn-list +73`, and `moon info && moon
  fmt`.

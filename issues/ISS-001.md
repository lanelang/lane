# ISS-001: Fix lane run for local recursive function example

## Metadata
- Type: bug
- Status: closed
- Priority: 2
- Labels: area/example, area/runtime, area/buslane, agent
- Assignee: unassigned
- Created: 2026-06-18
- Updated: 2026-06-18
- External ref: none

## Description
`examples/valid/04_blocks_and_local_scope.lane` is a valid example, but running the `factorial` entry currently returns a Buslane verifier/runtime diagnostic instead of a callable function value or successful executable result.

Observed command:

```sh
moon -q run --target native modules/lane -- run examples/valid/04_blocks_and_local_scope.lane:factorial --lib-dir ../basic
```

Observed output:

```text
invalid_program {
  unknown_value value#226
}
```

## Design
Investigate lowering for local recursive functions in block scope. The likely problem is that a local recursive binding references a value id that is not visible in the generated Buslane program. Keep the fix scoped to block/local-function lowering and verifier-facing metadata; do not weaken Buslane verification to accept missing value identities.

## Acceptance Criteria
- [x] `lane run` for `examples/valid/04_blocks_and_local_scope.lane --entry factorial` no longer prints `invalid_program`.
- [x] The fixture expectation in `examples/fixtures/lane-run.json` for this case is updated from the current diagnostic to the expected successful output.
- [x] `tools/check-lane-run-examples.sh` passes.
- [x] `moon test --target all --warn-list +73` passes.

## Relationships
- Depends on: none
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-06-18: Recorded from lane-run fixture expansion. The example is accepted by compiler tests but fails when executed through the CLI run path.
- 2026-06-18: Started diagnosis.

## Close Notes
- 2026-06-18: Fixed local function lowering to emit expression-level Buslane `letrec` so self-recursive local functions are visible while checking their RHS.
- 2026-06-18: Verified targeted command prints `<function> : (Int) -> Int`; `tools/check-lane-run-examples.sh` passes.
- 2026-06-18: Verified `moon test --target all --warn-list +73` passes.
